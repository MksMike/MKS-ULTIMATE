# Arquivos compilados MQL5
*.ex4
*.ex5

# Arquivos temporários do MetaTrader
*.log
Files/

# Windows
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# VS Code (configurações pessoais; extensões recomendadas ficam)
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json

# Claude Code — settings.json (allowlist compartilhada) fica versionado;
# settings.local.json é por-máquina (paths de %TEMP%, permissões locais)
# e não deve ser trackeado (M25, auditoria 2026-07-19).
.claude/settings.local.json

# Outras IDEs
.idea/

# Node.js (caso tooling seja adicionado)
node_modules/

# Python (caso scripts de análise sejam adicionados)
__pycache__/
*.pyc
.venv/
venv/

# Backups e temporários
*.bak
*.tmp
*~
.DS_Store
_compile_check.mq5

# Secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Dados de teste pesados
data/raw/
data/processed/
*.csv.backup

# Material de referência local — não vai pro repo público
# (Renko-MQL5 = referência community MQL5;
#  Renko-Ultimate = sub-pasta com code-review V5)
# V5 foi promovido para reference/V5/ — agora no repo.
RENKO-MQL5/
Renko-MQL5/
Renko-Ultimate/