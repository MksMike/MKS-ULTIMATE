# Changelog

Todas as mudanças relevantes neste projeto serão documentadas neste arquivo.

O formato segue [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/) e este projeto adere a [Versionamento Semântico](https://semver.org/lang/pt-BR/).

## [Não lançado]

### Fixed
- **CS — ColorReversal ganha auto-cura de container corrompido (paridade com o Producer) (2026-07-24).** Fecha o último caminho exposto da fragilidade "`CustomRatesDelete` falha → EA roda em cima do arquivo podre" (nomeada como follow-up do E7 em 2026-07-22). O Producer já auto-recuperava (`RecreateCorruptedCustomSymbol`); o **ColorReversal (a EA de TRADING) ignorava** o resultado do wipe ([ColorReversal.mq5:617](MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5#L617)) e seguia em cima do CS corrompido → `CS UPDATE FAIL` desde o brick #1, operador **preso sem sinal** (única saída era Ctrl+U manual). **Fix:** espelha o design validado do Producer — mede o **ERRO** do `CustomRatesDelete` (`ResetLastError`/`GetLastError`, **não** o retorno: CS vazio devolve `lastErr==0` benigno, corrompido devolve erro real ~4102); em erro, `RecreateCorruptedCustomSymbol` (fecha charts do CS via `ChartFirst/Next/Close` exceto o próprio, deseleciona do Market Watch, `CustomSymbolDelete`, recria limpo — **pré-condições confirmadas na doc MQL5**: "custom symbol displayed in Market Watch or with an open chart cannot be deleted"). **Diferença por PAPEL (não acidente):** se a auto-cura falhar, o Producer faz `INIT_FAILED` (só existe pra desenhar), mas o ColorReversal **Alerta alto e SEGUE SEM CS** — desliga via `csName` vazio (sink inerte por early-return), chart-open guardado, painter cai no chart do EA (`ChartID()`) com WARN; o CS é **visual-only (ADR-020) e NUNCA bloqueia o trading** (decisão do dono). Degradação graciosa de ponta a ponta verificada por leitura. `compile-all` **54/54 0/0**. Camada impura (APIs de CS não unit-testáveis). **Smoke MT5-verde (2026-07-24, demo Exness): ColorReversal anexou limpo, CS renderizou, 0 `CS UPDATE FAIL`** → não-regresso do caminho saudável confirmado. A auto-cura em si (só dispara com container podre) e a sobrevivência à meia-noite (E7.1) seguem como verificação futura; o caminho saudável (`wipeErr==0`) é byte-idêntico ao anterior. **Débito nomeado:** `RecreateCorruptedCustomSymbol`/`EnsureCustomSymbolReady`/`FindChartIdBySymbol` agora duplicados entre os 2 EAs — candidato a extração DRY p/ include compartilhado (fatia futura; não tocar o Producer validado agora).

### Fixed
- **E7.2 — robustez do sink do CS na falha do `CustomRatesUpdate` (2026-07-24).** Fecha os 4 achados do E7.2 do `ROADMAP-CORE-HARDENING.md` (todos na camada de desenho, o ponto cego thin-em-teste). (1) `[cs-recovery-advances-timeline-on-fail]` — em falha do `CustomRatesUpdate`, `nextBarTime` **ainda avançava** (+60/brick), derivando a timeline pro futuro e alimentando o runaway do bump que matou o CS; `lastBarTime` já era success-only desde 2026-07-22, mas `nextBarTime` não. **Fix:** a transição da timeline vira função pura `ApplyCloseTimeline(ok, brickTime, &lastBarTime, &nextBarTime)` — **ambos** só avançam no SUCESSO; em falha mantêm o slot da última barra REAL (painter da ADR-028 não ancora em slot vazio). Não congela nada: no próximo brick de mercado calmo/recuperado o `realTime` volta a vencer em `ComputeBrickTime`. +3 testes de regressão (`Test_ApplyCloseTimeline_*`: sucesso avança ambos; falha mantém ambos; sequência sucesso→falha→sucesso reusa o slot sem abrir buraco) — **puros, rodam sem CS**. (2) `[cs-forming-no-recovery-asymmetry]` — `OnBrickForming` (roda a cada tick, sinal MAIS PRECOCE da morte do CS) não logava nenhuma falha; agora loga **a 1ª** via contador dedicado `formingUpdateFailures` (independente do close, capturando o instante exato da recusa sem poluir por tick). (3) `[cs-forming-bar-orphan-vs-doc]` — headers do sink diziam "OnBrickClose sobrescreve este slot" (só verdade no bump/mercado frenético); corrigidos para refletir que em mercado calmo (realTime vence) o close grava num slot à frente e a parcial fica **órfã** no slot anterior (aceito, ADR-023 r.3). (4) `[cs-painter-discards-real-timemsc]` — header do `CMksChartPainter` narrava âncora "no tempo real do tick"; corrigido para o comportamento real do `AnchorTime` (live = `sink.lastBarTime`, o slot do CS; tester = `timeMsc/1000`). Só comportamento em falha do CS + logs + comentários; **paridade intocada** (o CS é visual-only, ADR-020). `compile-all` **54/54 0/0**. **MT5-verde (2026-07-24): `Test_CMksCustomSymbolSink_Timeline` 529/529 assertions, 15 tests, 0 failed** ✅ (12 antigos + 3 novos `Test_ApplyCloseTimeline_*`). **E7.2 fechado; E7.1 (sobrevivência à meia-noite, pende de dado) e E7.3 (ADR-031) seguem abertos.**

### Changed
- **E6.1/E6.3/ADR-037 — superfície de inputs enxuta (3 níveis) + presets por instrumento no ColorReversal (2026-07-21).** O `ColorReversal.mq5` saía de **~31 inputs** num nível plano. Agora: **BÁSICO** (instrumento: preset/brick/SL; lote/risco: modo+lote/pct+3 limites de conta ≈8), **AVANÇADO** (risco fino/histórico), **DIAGNÓSTICO** (paridade/captura/visualização). (a) **K/L saem do dialog** `[k-l-defaults-coupled]`: `InpInvalidTickLimit`(L)/`InpThresholdLimit`(K) viram const de arquivo (`kInvalidTickLimit=10`/`kThresholdLimit=20`), **valores mantidos** → golden do E2 intacto; **NÃO** auto-derivei K (mudaria o valor → risco de paridade). (b) **Preset por instrumento** (`InpInstrumentPreset` Custom/XAU/EURUSD): `!= Custom` **sobrepõe** brick/SL/pisos no OnInit (ponto único → `eff*`, precedência **logada**); Custom usa os inputs. XAU = brick 3.0 / SL 10 bricks (produção); EURUSD = 0.0010 / 10 (inicial, a ajustar com dado). (c) **E6.2** já fechado pela ADR-032 (SL em bricks; `InpMinSlFloorPts` é legitimamente pontos). Fecha `[input-surface-too-large]`. **Preset=Custom (default) → comportamento IDÊNTICO ao anterior.** Compila 0/0 (48 arquivos). **MT5-verificado (2026-07-21):** (a) dialog nos 3 níveis com K/L ausentes — confirmado no dialog; (b) preset XAU **sobrepõe** o input — anexado com `InpBrickSize=99` + preset XAU, log `starting` saiu `"preset":"XAU","S":3.00000,"slBricks":10.00` e o CS virou `XAUUSDm.MKSCR_3` (não `_99`); (c) Custom preserva — run com preset Custom + brick 99 saiu `"preset":"custom","S":99.00000`; OnInit limpo (historical fill + `ready for ticks`, 0 erro) nos 3 runs. **Não re-exercitado:** item (d) — tester com abre/fecha de ordem — fora do diff da ADR-037 (mudança confinada a input→`eff*`; order path byte-idêntico, já provado na Fase 9 tester 617/617 e no E5 52/52). **Sub-item E6.1 fechado (2026-07-21):** o default de `InpHistoricalFillDays` **difere por PAPEL** — Producer=30 (visualização), ColorReversal=3 (warm-up; estratégia não opera no histórico), DecisionReplayer=0 (paridade) — decisão de **documentar** (rationale cruzado nos 3 sites), não unificar (seria arbitrário; o operador só vê o input do ColorReversal, e o footgun "recording exige 0" já é pego por `Alert`). **E6.1 completo.**

### Changed
- **E2 FECHADO — decisão de cadência/baseline do snapshot resolvida (2026-07-24).** Último item pendente do E2 (gate para a Fase 10). O **núcleo** do achado de paridade do clock (`parity-mt5clock-wallclock-leak` + `parity-mt5clock-isready-asymmetry`, ambos rooted no `CMksMt5Clock`) foi **resolvido pelo E2.4**: live (`ColorReversal`) e replay (`CMksDecisionRunner`) usam **ambos** `CMksFeedClock` (feed-driven) para a fronteira de dia UTC do snapshot — grep confirma que `CMksMt5Clock` não está em **nenhum** composition root de decisão (só no seu smoke test). **Residual aceito e documentado:** o live seedeia o baseline do último tick pré-attach (`SymbolInfoTick`) + `Init()` no `OnInit`; o replay faz `Init()` lazy no 1º tick do `.mkstick` — diferem só no tick exato, **mesmo dia UTC no attach**, auto-corrige via rollover; a paridade **sim↔sim** (`Test_DecisionGolden`) é bit-exata e live↔replay é estruturalmente não-bit-exato. Endurecimento (lazy-init no `ColorReversal` para simetria total) diferido — sem ganho de paridade hoje. Só docs + comentário. **Gate E1–E5 formalmente aberto para a Fase 10.**

### Added
- **E2 — golden de DECISÃO headless (`Test_DecisionGolden`) (2026-07-24).** Automatiza a regressão da paridade de decisão que hoje é manual (`verify-parity.ps1`): roda o `CMksDecisionRunner` sobre o fixture real (`tests/golden/e2-decision/`, config A = defaults do `MksDecisionRunnerConfig` + `slPoints=30000`, gates OFF) e compara o journal produzido contra `baseline.golden.tsv`, **ignorando as linhas `#`** (proveniência/timestamps — mesma normalização do `verify-parity`), falhando o `TestRunner` na 1ª divergência (com dump da linha). O fixture e o golden vivem no repo e são copiados para `MQL5\Files\MKS-ULTIMATE\golden\` no setup (`Files/` é gitignorado). Descoberta ao mapear a config: só `slPoints` difere do default do struct — todo o resto da config A (brick 3.0, L=10, K=20, FIXED 0.01, custos 0, gates 0) **já é default**. **Config B (gate-crossing) também automatizada:** `Test_DecisionGolden_MatchesGate` (`minEquityAbs=9990`) vs `gate-minequity.golden.tsv` (1 trade + 16 SEND REJECTED 409) — robusto ao `tickValue` exato (a partir do trip do gate todo SEND é rejeitado; só o 1º trade precisa disparar). compile-all 54/54. **Config A MT5-verde 5/5** (casou de 1ª); config B MT5-verde pendente.
- **E2.4 — verificação da paridade cruzando a meia-noite UTC (rollover feed-driven) (2026-07-24).** `Test_Runner_MidnightRolloverDeterministic` (em `Test_CMksDecisionRunner`) prova que a fronteira de dia UTC do `CMksAccountSnapshot` é dirigida pelo **`timeMsc` do feed** (`CMksFeedClock.SetNow` por tick), **não** pelo wall-clock (`TimeCurrent`): um stream de ticks cruzando 00:00 UTC (feed-time ~2026-05-23, deliberadamente ≠ do wall-clock — se o código usasse `TimeCurrent()` o rollover não dispararia e o teste falharia) **rola o baseline no feed-midnight** (assert `DayStartMsc()` salta do dia pré → pós), e dois runs do mesmo stream produzem journals **byte-a-byte idênticos atravessando a virada**. O código do clock derivado do feed já estava **simétrico nos dois lados** (ColorReversal live + DecisionRunner replay ambos injetam `CMksFeedClock` no snapshot). Getter `DayStartMsc()` adicionado ao runner (observabilidade/teste). compile-all 53/53; MT5-verde do teste pendente do dono. Pendente do E2: automação headless do golden de DECISÃO (hoje `verify-parity` manual) + decisão da cadência do snapshot.
- **E2.3 — âncora de proveniência (`seedMid`/`seedTickSeq`) no header do `.mksbk` + fail-fast de paridade (2026-07-24).** A "âncora da escada" (mid + seq do 1º tick que semeia o builder) é gravada no header do `.mksbk` para o reader validar que **duas séries partem do MESMO ponto** antes de compará-las (paridade à prova de operador). **Forward-compatible dentro da ADR-024** (não é ADR nova): dois campos no espaço antes reservado — `seedMid` (double) @192, `seedTickSeq` (int64) @200; `formatVersion` segue **1**; arquivos pré-E2.3 leem **0/0 = âncora ausente** (`HasSeed()` false). A seed só existe após o 1º tick → é **patchada no `Close`/`Checkpoint`** (padrão do `brickCount`), via `CMksBrickFileWriter.SetSeed()` chamado pelo composition root a partir do builder (`CMksRenkoBuilder.SeedMid()/SeedTickSeq()/HasSeed()`, capturados na 1ª init, **imutáveis no reanchor de gap**). Wiring em Producer (OnDeinit + Checkpoint) e Replayer (FinishReplay + OnDeinit). Companheiro: `InpParityRunMode` no Producer agora **fail-fast** se `InpHistoricalFillDays != 0` (o `.mksbk` de paridade só pode ter bricks de ticks live). **Verificado adversarialmente** (workflow 3 lentes — backward-compat/formato, semântica/paridade, consumidores/completude — todas `safe`, **0 regressões**): `verify-parity` **fortalecido** (pega divergência de origem), nenhum golden quebra (comparados por records), Producer↔Replayer produzem seed idêntica por construção. Ajustes aplicados dos veredictos: `HasSeed` robusto a seq 0-based; `SetSeed` antes do Checkpoint (crash-tolerance). +3 testes (`Test_SeedRoundtrip`, `Test_SeedAbsentWhenNotSet`, `Test_SeedCaptured`); nota na ADR-024. Diferido (só `InpSizerMode=ATR`): snapshot do sizer. compile-all 53/53; MT5-verde dos 3 testes pendente do dono.
- **E2.2 — golden de bricks headless (paridade feed→brick como rede de regressão automática) (2026-07-23).** `Test_RealTickGolden.mq5` (Layer A): lê o fixture `.mkstick` real via `CMksFileTickSource` (hermético, **não** `CopyTicksRange`), roda o `CMksRenkoBuilder` **real** (produtor único — wiring idêntico ao Replayer: sizer 3.0, geometria classic, L=10, K=20) e compara os bricks **campo-a-campo EXATO** (incl. `triggerPrice`, `triggerTickId`, `high`, `low`, `closeTimeMsc`) contra um `.mksbk` golden versionado (`tests/golden/e2-brick/`), falhando o TestRunner em qualquer divergência. `InpRegenGolden=true` gera o golden via o **mesmo writer** do Producer/Replayer. Compara records (não bytes crus) → ignora o `createdAt` wall-clock. **Golden abençoado:** 32 bricks, contiguidade perfeita, **17 flips == os 17 flips do golden da decisão já validado** (`verify-parity` exit 0) → bricks comprovadamente corretos, `triggerTickId>0` em 32/32. **MT5-verde: 10/10 assertions, 0 failed.** `Test_Producer` rebaixado a cross-check manual (não-hermético, superado). Cumpre o 2º critério de saída do E2. compile-all 53/53. Falta E2.3 (âncora de proveniência) + E2.4 (meia-noite UTC) pra fechar o E2 e destravar a Fase 10.
- **E3 e E4 fechados — gate E1–E5 agora a UM item (E2) da Fase 10 (2026-07-23).** (a) **E4.2 (eixo 3 em MOEDA):** `Test_SR_SlippageDegradesNetCurrency` prova que `slip>0` reduz **estritamente** o `netPnLCurrency` (não só `netPnLPoints`) — dois runs idênticos exceto slippage, comissão `3.0/lote` nos dois exercita o caminho de moeda completo (`hasCurrency`+`totalCommission>0` assertados). Fecha o último sub-teste do E4 → **E4 concluída** (MT5-verde do novo teste pendente do dono). (b) **E3 (processo):** Protocolo 1 §determinismo reforçado — exige **teste duplo-run automatizado** (roda 2× e compara, não inspeção/"determinismo assumido"), motivado pelo achado `[H2]` (soft-recovery com determinismo afirmado e nunca testado). Cumpre o 3º critério de saída → **E3 concluída**. `compile-all` 52/52. Restam só os itens de **E2** (paridade de decisão) para destravar a Fase 10.
- **ADR-041 proposta: distância mínima de SL para gestão (BE/trailing) ancorada em bricks (2026-07-23).** Fecha o follow-up #1 da auditoria 2026-07-22 (§10), descoberto na verificação adversarial do Lote C-fix. O `CMksTradeManager` move o SL de BE/trail via `broker.Modify()`; o broker live exige `SYMBOL_TRADE_STOPS_LEVEL`, o `CMksSimulatedBroker` não modela → SL perto demais é rejeitado no live (mantém SL antigo) mas aplicado no backtest = divergência (eixo 2 do V5). **Decisão:** floor de distância mínima ancorado em bricks (`InpManageMinSlBricks`, convertido a `manageMinSlPoints` no composition root, contrato do TM segue em pontos), enforced em **config-time** no `Validate()` (BE: `beActivation−beOffset ≥ floor`; trail: `trailStep ≥ floor`); `StopsLevel` fica FORA do runtime (só fail-fast de anexação) — espelha M12/ADR-032. Default `0.0` (desligado → compat retroativa); furo dormente hoje (TM fora do caminho live). Rejeitadas: StopsLevel em runtime, modelar StopsLevel no sim, clamp em runtime, só-documentar. Passa nos 5 invariantes (reforça a paridade). **Status: Aceita (2026-07-23).** **Núcleo implementado:** `manageMinSlPoints` em `CMksTradeManagerParams` (default 0.0) + checks no `Validate()` (BE: `beActivation−beOffset ≥ floor`; trail: `trailStep ≥ floor`; `manageMinSlPoints ≥ 0`) + 4 testes. O input do operador `InpManageMinSlBricks` + conversão bricks→pontos ficam **diferidos ao wiring do TM num EA** (hoje o TM só existe em testes — input num EA que não constrói os params seria morto).
- **ADR-040 proposta: circuit breaker CORRETIVO (flatten-on-breach) (2026-07-22).** Resolve a decisão pendente da ADR-036 (E5.4). O breaker Por Conta era só PREVENTIVO (bloqueia abertura, códigos 407–409); mas o V5 quebrou a conta com posição ABERTA. Agora: (a) **predicado único** — os 3 checks Por Conta extraídos em `CMksRiskManager.AccountBreached()` (público) + `AccountBreachPredicate()` (privado), reusados pelo `CheckOrder` (preventivo, **behavior-preserving**) e pelo breaker (corretivo) → fonte única, zero divergência; (b) **`CMksCircuitBreaker : IBroker`** (decorator outermost): `Send` REJECTED sticky se tripado, `Close`/`Modify` passam, `OnTick()` detecta breach → tripa (sticky) + flatten (itera `IPositionBook.PositionAt`→`inner.Close`, retry por tick); (c) **sticky** (trava até `Reset()`, não re-arma no rollover/recuperação); estratégia cega (leva REJECTED, auto-detach da posição fechada). `+Test_CMksCircuitBreaker` (trip+flatten+block+sticky+reset; saudável não tripa; risk NULL inerte). **Integrado no `ColorReversal.mq5`**: input `InpEnableCorrectiveBreaker` (default ON), breaker envolve o gate (broker mais externo), `OnTick()` por tick antes do `IngestOne`. Muda o comportamento live (fecha+trava no breach vs só bloquear abertura); desligar volta ao só-preventivo. Compila 52/0/0. **MT5-verde (2026-07-22):** `Test_CMksCircuitBreaker` 18/18 (3 tests) + `Test_CMksRiskManager` 119/119 (70 tests, refactor SEM regressão) + `Test_CMksRiskGatedBroker` 23/23 (10 tests). Smoke da integração no ColorReversal (breaker disparando live) = demo opcional. **Status: Proposta.**
- **ADR-039 proposta: calibração do StressLab ao broker real — spread medido + ancorado (2026-07-21).** Parser `.mkstick` fora do MT5 sobre 417k ticks reais XAUUSDm/Exness → **spread baseline = 240 pts (0.24 USD)**, muito estável (p95 260, p99 280, pico 700). Ancorado no `StressReplayer`: `InpSpreadPts=240` (CostModel, half=120/lado) + `InpBaselineSpreadPts=240` (composição do stress). Multiplicadores dos presets mantidos — já mapeiam realista (High 3×≈720 ≈ pico real). **Latência→drift MEDIDO e LIGADO** (~0.25 pts/ms adverso @ ~260ms, via deslocamento líquido `|mid(t+L)−mid(t)|`, NÃO a velocidade ingênua que superestimava 20–50×): `latencyDriftPointsPerMs` entrou no `MksStressRunnerConfig` + input `InpLatencyDriftPtsPerMs=0.25` (tunável) no `StressReplayer`; modelo = expected-adverse; `+Test_SR_LatencyDriftDegradesVsOff`. **Só o slippage puro segue PENDENTE** (precisa de dado requested-vs-filled real; só 11 trades do demo). Compila 51/0/0. **Status: Proposta.**
- **ADR-038 aceita: runner de estresse liga/desliga (slice-2 da Fase 9 / Parte B) (2026-07-21).** Desenho aprovado para 3 peças novas — `CMksTradeJournalingBroker` (decorator `IBroker` que alimenta o `CMksTradeJournal` no Send/Close FILLED; auto-close de SL entra pelo `PollAutoCloses`, sem dupla contagem), `CMksStressRunner` (orquestrador dono do grafo por nível: `CostModel→SimulatedBroker→StressLabBroker→RiskGatedBroker`+journaling+estratégia+builder, captura 1 `CMksStressLabReport`/nível), `StressReplayer.mq5` (EA que loopa None→Nightmare sobre 1 `.mkstick` e imprime `MksStressLabPrintComparison`). Liga/desliga = None vs Light→Nightmare numa corrida (≤2 passos). Testes antes do EA. Calibração ao broker real = fatia separada (dívida nomeada). Passa nos 5 invariantes. **Status: Aceita** (aceite do dono após o smoke verde, 2026-07-21). **Fatias 1-3 code-complete + compilam 51/0/0 (2026-07-21):** `CMksTradeJournalingBroker` (+8 testes), `CMksStressRunner` (+4 testes), `StressReplayer.mq5` (EA: N runners em paralelo numa passada pelo `.mkstick` → `MksStressLabPrintComparison`; `baselineSpreadPts` default 0 = spread-stress inerte até calibrar). **Fatias 1-2 MT5-verificadas (2026-07-21):** `Test_CMksTradeJournalingBroker` 27/27 (8 testes) + `Test_CMksStressRunner` 17/17 (4 testes), 0 falhas — Init monta o grafo, feed gera trades, duplo-run determinístico, slippage degrada o net. **Smoke MT5-verde (2026-07-21) — slice-2 da Fase 9 / Parte B FECHADO:** StressReplayer sobre o golden (33.546 ticks, 32 bricks, 17 flips, 5 níveis, 219ms, self-remove limpo). Tabela comparativa: None 16 trades net −10661 pts slip 0; Light/Medium/High **mesmos 16 trades**, slippage monotônico (21→64→154 pts) e net degradando (marcador `v`); **Nightmare 9 trades net +1777 pts** — as 7 rejeições bloquearam entradas e, numa estratégia perdedora (PF 0.659), *operar menos* melhorou o net → artefato que ilustra `robustez ≠ lucratividade`, não robustez real. O mecanismo (degradação por slippage + bloqueio por rejeição + tabela comparativa) está provado sobre feed real. Calibração ao broker real segue como fatia 5 (dívida nomeada).
- **E6.4 (parcial) — runbook de primeira execução + header do ColorReversal corrigido (2026-07-21).** Início da Fase E6 (produto operável). (a) **Runbook de 1 página** no `CHEATSHEET.md §9.8`: tabela "qual EA para quê" (ColorReversal opera; Producer só visualiza; TickRecorder captura; Replayer/DecisionReplayer replaya; Test_* como Scripts), pré-requisitos (conta HEDGING, símbolo REAL, junctions+compile), passo a passo do live, e o mapa das mensagens de recusa do `OnInit` → causa (fecha `[run-ea-flow-manual-multistep]`). (b) **Header do `ColorReversal.mq5`** corrigido: narrava um "runner stress" reutilizando `SimulatedBroker + StressLabBroker` como se fosse iminente; agora diz que o EA só OPERA, que a paridade de decisão tem EA próprio (DecisionReplayer) e que o runner de stress **não existe** (fatia futura) — fecha `[header-describes-nonexistent-stress-runner]`. Só docs/comentário; compila 0/0 (48 arquivos). **E6.4 — decisão do Producer:** manter como EA separado (dev/visualização), decisão do dono 2026-07-21. E6.1/E6.2/E6.3 feitos na sequência (ver entrada `Changed` acima, ADR-037).

### Fixed
- **Lote A da auditoria 2026-07-22 — 5 fixes de correctness (CS + Producer + indicadores + core defensivo).** (1) `Producer.mq5:771` → `g_nextBarTime=0` (ADR-023 regra 1) — **mata a raiz do desalinhamento do CS** (a série do fill saía ~6h no futuro pelo bump+teto). (2) `Producer.mq5` → `FolderCreate("MKS-ULTIMATE\\Ticks")` no modo paridade — o `.mkstick` não abria em máquina limpa (`INIT_FAILED`; os outros 2 escritores criam a pasta, o Producer esquecia). (3) `CMksDonchian`/`CMksChandelier`/`CMksSuperTrend` limpam o warm-up `[0..start-1]` com `EMPTY_VALUE` (buffers novos = 0.0 plotavam **mergulho até zero** no canto do chart; RSI/MACD já faziam — simetria). (4) `CMksAtrBrickSizer.Validate` rejeita `defaultSize` fora de `[minSize,maxSize]` (o size de warm-up, sem clamp, alimenta a ESTRATÉGIA e furava o teto/piso) `+2 testes`; `CMksRenkoBuilder` valida a geometria no ctor → geometria inválida marca `m_streamCorrupt` (recusa ALTA em vez de consumir ticks sem nunca emitir brick). (5) `CMksCustomSymbolSink:197` → `lastBarTime` só avança em barra **efetivamente gravada** (`CustomRatesUpdate` OK); o marcador da ADR-028 não ancora mais em slot sem barra; `nextBarTime` segue avançando pra não travar a timeline. **Sem regressão** (verificado: nenhum teste constrói config/geometria que os guards novos rejeitem — median 0.5 e custom 0.99 passam; defaults do sizer em range). Compila 52/0/0. MT5-verde das suítes pendente do dono.
- **CS — a barra de FORMAÇÃO agora respeita `showWicks` (2026-07-22).** Achado do dono: o CS mostrava **pavios mesmo com `InpShowWicksInCS=false`**. O `OnBrickClose` respeitava o flag (caixinha limpa), mas o `OnBrickForming` escrevia SEMPRE `fb.high`/`fb.low` (excursão) — então a barra parcial viva **E** as parciais **órfãs de slot** (que sobram quando o box fecha num minuto diferente da formação) apareciam com pavio, "quebrando" a sequência de caixinhas. **Fix:** `OnBrickForming` respeita `showWicks` igual ao `OnBrickClose` (corpo da caixinha via `BoxBodyHigh/Low`, extraído como helper puro DRY entre os dois + testado). O `.mksbk` preserva a excursão real (ADR-020). Compila 52/0/0; +teste `Test_BoxBodyBoundsAreBodyExtremes`. **Fronteira honesta:** isso mata os PAVIOS; se ainda restar desalinhamento HORIZONTAL (caixinhas limpas mas com buraco no tempo), é a tensão do eixo-de-tempo híbrido (ADR-023-A) — conserto de raiz = eixo por índice (E7/E8, decisão em aberto).
- **CS (Custom Symbol) — guarda de `time > 0` mata a corrupção "invalid container (1970.01.01)" (2026-07-22).** Achado do dono explorando brick sizes: com brick pequeno (ex.: 1 USD no XAU) o CS `XAUUSDm.MKS_1` **congelava**; o Journal do MT5 mostrava `HistoryBase: invalid container (1970.01.01)` — barras gravadas em **time=0 (epoch)** corrompendo o container. **Causa-raiz:** `CMksCustomSymbolSink.OnBrickForming` gravava `rates[0].time = nextBarTime` **sem guardar contra `nextBarTime==0`**; e `nextBarTime` só continua 0 se o fill fechou **zero bricks** (então o forming live despeja barras em 1970). O `OnBrickClose` **não** é a fonte: `ComputeBrickTime(closeTimeMsc>0)` sempre retorna >0 (provado em `Test_FirstBrickFromEpochZero`). **Fix (visual-only, ADR-020 — não toca paridade):** guarda pura `IsRenderableBarTime(t)=t>0` na fronteira de escrita — `OnBrickForming` pula a barra parcial + **falha alto uma vez** (`[CS] forming suprimido: nenhum brick fechou ainda`) em vez de corromper em silêncio; `OnBrickClose` ganha a mesma defesa (nunca dispara p/ tick real; não dropa brick legítimo). O `.mksbk` segue com o brick intacto. `+2 testes` (`Test_GuardRejectsEpochZeroBarTime`, `Test_CloseNeverEmitsEpochForValidTick`). Compila 52/0/0; **MT5-verde:** `Test_CMksCustomSymbolSink_Timeline` 515/515 (11 testes, 0 falhas). **RESOLVIDO E CONFIRMADO NO MT5 (2026-07-22):** a guarda é o root-cause fix. Diagnóstico final (o "Bug B" foi DESCARTADO — o fill de brick 1.0 emite `bricks:1851`, `writeFailures:0` no `.mksbk`; o motor sempre esteve certo): o arquivo do CS `MKS_1` estava **corrompido de uma run anterior** (barras em 1970 escritas antes da guarda existir); daí o `CustomRatesUpdate` falhava desde o brick #1 (`CS UPDATE FAIL`, lastErr=0) e o `CustomRatesDelete`/reset não conseguia limpar (err 4102). A guarda impede a corrupção **recorrer**, mas **não cura** o arquivo já podre. Após **apagar o símbolo inteiro** (Ctrl+U → Delete — o `InpResetCustomSymbolBars` NÃO basta, pois usa `CustomRatesDelete`, que falha no arquivo corrompido) e re-rodar com a guarda ativa, o brick 1.0 renderiza. **Fragilidade remanescente (E7, follow-up):** quando o `CustomRatesDelete` falha, o Producer só loga warning e segue em cima do arquivo podre — deveria `CustomSymbolDelete`+recriar limpo (ou Alert alto) pra o operador nunca ficar preso.
- **E5.3/ADR-036 — flip do ColorReversal NÃO abre nova posição se o Close falhou (evita exposição dupla) (2026-07-21).** No `CMksColorReversalStrategy.OnBrickClose`, o flip chamava `CloseCurrentIfAny()` (que **zerava o vínculo mesmo em falha**) e **ignorava o retorno**, abrindo a nova posição sempre. Close falho no flip → posição antiga seguia ABERTA no broker mas órfã + nova posição = **exposição dupla** `[close-failure-clears-state-still-opens]` (com `maxOpenPositions=1` o gate mascarava, mas a estratégia perdia o track da órfã; sem limite, dupla real). **Fix:** `CloseCurrentIfAny` **mantém** o vínculo na falha (só zera em `FILLED`), e o `OnBrickClose` só abre a nova se `m_currentPositionId==0` após o fecho — senão loga WARN e não abre; o próximo flip / auto-detach / operador reconciliam. Simétrico ao `FlattenForSafety` (que já mantinha o vínculo na falha). Nenhum teste existente exercitava o flip-close falho → sem regressão; +2 testes (`Test_CR_FlipCloseFailureKeepsPositionNoNewEntry`, `Test_CR_FlipCloseFailureThenNextFlipReconciles`).

### Changed
- **E5.4/ADR-036 — semântica do circuit breaker (preventivo) e do DayPnL (equity flutuante) documentada (2026-07-21).** Só docs/comentários — sem mudança de comportamento. (a) **Breaker é PREVENTIVO** `[circuit-breaker-only-gates-opening]`: a camada Por Conta do `CMksRiskManager` roda só em `CheckOrder` (na abertura/Send) → bloqueia novas entradas, NÃO fecha posição aberta ao cruzar o limite. Um componente **corretivo** (`flatten-on-breach`: fecha tudo + trava via `OnTick`) fica como **decisão em aberto** (decisão do dono: só documentar por ora) — registrada no comentário do `CMksRiskAccountParams` e na §4 do ARCHITECTURE. (b) **DayPnL usa equity FLUTUANTE** `[daypnl-uses-equity]`: `Equity() − dayStartBalance` (numerador inclui não-realizado; baseline = balance realizado no rollover UTC, `[rollover-baseline]`) — escolha deliberada e mais protetora (reage a drawdown intraday), agora documentada no `CMksAccountSnapshot::DayPnL`.
  - **Fecha a Fase E5** (com a ADR-035) → gate E1–E5 cumprido, destrava a Fase 10. **MT5-verificado (2026-07-21): `Test_CMksColorReversalStrategy` 22 tests, 0 failed** ✅ (2 novos do E5.3 + 20 existentes, sem regressão). Arquivos do E5.3/E5.4 estão DENTRO do grafo do ColorReversal (recompila o EA) — feito sem captura viva; `compile-all` 48 arquivos 0/0.

### Fixed
- **E5.2/ADR-035 — partial close do `CMksTradeManager` acumula fill parcial (não re-fecha o alvo sobre o residual) (2026-07-21).** No `Update()`, o partial só marcava `m_partialDone` em `MKS_EXEC_FILLED`. Um `MKS_EXEC_PARTIAL` caía no ramo de WARN e deixava `m_partialDone=false` → no próximo tick elegível re-emitia `Close(alvo INTEIRO)` sobre o residual → **over-close** (ou loop de rejeições quando o alvo passa a exceder os lots restantes) `[partial-close-partial-status-reapplies]`. Latente: nenhum broker do projeto devolvia `PARTIAL`, mas um broker live pode. **Fix:** novo acumulador `m_partialFilledLots` — o alvo (`lots iniciais·pct`) é fechado UMA vez, re-emitindo só o **residual** (`alvo − acumulado`) a cada tick elegível até o alvo; `PARTIAL` é progresso (acumula `r.filledLots`), `m_partialDone` só vira `true` quando o acumulado ≥ alvo; zero-fill num status "ok" cai no WARN (não acumula). Getter `PartialFilledLots()`; reset no `Attach`/ctor. Não-breaking (FILLED integral fecha num passo, como antes) — os 28 testes do `Test_CMksTradeManager` seguem válidos (**MT5: 74/74 assertions, 28 tests, 0 failed** ✅).

### Added
- **E5.1/ADR-035 — `CMksTradeManager` provado num composition root REAL + hook de fill parcial no sim (2026-07-21).** Fecha `[trademanager-not-wired-in-ea]`: o TradeManager tinha 29 unit tests mas **todos contra o mock** `CMksRecordingBroker` — nenhuma estratégia do projeto usa gestão de trade (o `ColorReversal` é reversão pura), então BE/trailing/partial nunca rodaram contra fills reais do `CMksSimulatedBroker`, o auto-close de SL/TP, o auto-detach via `CMksSimPositionBook`, nem o `CMksRiskGatedBroker`. A Fase 10 vai compor exatamente essa cadeia.
  - `Scripts/.../Tests/Test_TradeManagerIntegration.mq5` — **novo** (7 testes) compondo a cadeia REAL (`CostModel → SimulatedBroker → RiskGatedBroker(+RiskManager+FixedLotSizer+SimPositionBook) → TradeManager`): (a) BE+partial+trail no mesmo tick tocam o estado do **simulador** (SL movido, lots reduzido) + idempotência; (b) o BE move o SL do broker e um recuo de preço auto-fecha no sim → o TM **auto-detacha** sem broker fantasma (evento drenável via `PollAutoCloses`); (c) o gate rejeita 2ª abertura (`maxOpenPositions`) e abertura sem SL → sem posição fantasma, TM não vinculado; (d) fill parcial **acumula e fecha exatamente o alvo** (regressão do E5.2); (e) determinismo duplo-run do caminho integrado.
  - `Core/Broker/CMksSimulatedBroker.mqh` — `SetNextCloseStatus(status, filledLots)`: injeção determinística **one-shot** (sem RNG, consumida após um uso) que força o próximo Close a devolver `PARTIAL` (fill < pedido, residual segue aberto) / `REJECTED` / `ERROR`. Modela um fill de fechamento parcial — comportamento real de broker que o sim (sempre integral) não representava. Default `FILLED` = inerte → zero impacto nos testes existentes.
  - Compila **0/0** (`compile-all`: **48 arquivos** limpos). **MT5-verificado (2026-07-21): `Test_TradeManagerIntegration` 52/52 assertions (7 tests), `Test_CMksTradeManager` 74/74 (28 tests), `Test_CMksSimulatedBroker` 83/83 (20 tests), 0 failed** ✅ (o hook one-shot no sim não regride as suítes existentes). E5.1/E5.2 fecham o critério "BE/trailing/partial fim a fim + parcial + idempotência"; faltam **E5.3/E5.4** (tocam o grafo do ColorReversal) para fechar a Fase E5.

### Added
- **E2.1 — determinismo da DECISÃO sobre feed REAL PROVADO (`verify-parity` exit 0) (2026-07-21). 🎯 Marco central do E2.** O `ColorReversal` capturou **33.546 ticks reais** do XAUUSDm (Exness, digits=3) num `.mkstick`; o `DecisionReplayer` (config `InpSlBricks=10`) montou o grafo DDR e produziu um decision journal de **33 decisões / 17 flips** (`ticksInvalid:0, ticksK102:0`, sem halt, self-remove no EOF). **Dois replays independentes do MESMO `.mkstick` → `verify-parity` exit 0: "decision journals IDÊNTICOS (33 decisões)".** O determinismo da camada de decisão — a camada onde o V5 quebrou a conta — deixa de ser teorema e vira **fato demonstrado sobre feed real**. Confirma de passagem o ADR-032 em produção (`slPts:30000` = 10 bricks = 30 USD em digits=3). **Reframe honesto mantido:** prova determinismo sim↔sim (runner↔runner), NÃO paridade live-broker↔replay (estruturalmente impossível).
- **E2.2 (golden bundle) — gate-crossing provado + bundle versionado (2026-07-21).** O MESMO fixture, replayado com **`InpMinEquityAbs=9990`**, faz o **breaker de conta disparar** (código 409 `min_equity_breached`) após o 1º trade e rejeitar todo Send seguinte — a proteção que faltou no V5, agora exercitada. Dois replays dessa config → `verify-parity` **exit 0 (18 decisões idênticas)**. O golden bundle está versionado em **`tests/golden/e2-decision/`**: o `.mkstick` (33.5k ticks reais), os dois journals golden (`baseline.golden.tsv` 33 decisões, `gate-minequity.golden.tsv` 18 decisões) e um `README.md` com as duas configs + a reprodução (`verify-parity` run↔run e run↔golden). **Falta só** o teste headless automatizado do golden da decisão (hoje a verificação é o procedimento manual + `verify-parity`; o fixture precisaria ir pra `MQL5\Files\` no setup, pois `Files/` é gitignorado).

### Fixed
- **E4.1/ADR-034 — comissão deixa de ser computada-e-descartada no caminho StressLab (eixo 3 do V5) (2026-07-21).** A comissão era computada (`CMksCostModel`→`CMksSimulatedBroker`: `r.commission`, `pos.commissionOpen`, `ev.commissionClose`) mas **descartada** em `SimulatedBroker→CMksTradeJournal→CMksStressLabReport` — o journal só trabalhava em pontos, `RecordClose` não recebia comissão, o report só reportava pontos. Era o padrão exato do eixo 3 do V5 (custo num relatório, nunca no resultado), latente na métrica que "diz pra ir pra live". **Fix:** `CMksTradeJournal` vira money-aware (`SetMoneyConversion(tickSize, tickValue)`, fórmula idêntica ao `CMksSimAccount`) + campos `commission`/`swap` em `MksClosedTrade` + params de comissão com **default `0.0`** em `RecordOpen`/`RecordClose` (não-breaking) + `NetPnLCurrency() = bruto − comissão − swap`. `CMksStressLabReport` ganha `netPnLCurrency`/`totalCommission`/`hasCurrency` e os surfacea no `ToJsonLine`/`PrintComparison`. A integração do report passa a **repassar `commissionClose`** do auto-close (antes descartado). **Swap segue OFF (v1, ADR-030)** mas a fórmula é swap-aware (0). (Nota: o caminho E2/`CMksSimAccount` já aplicava comissão ao equity; o gap era só a dupla journal/report.)
  - **E4.3 — warning de spread inerte:** `CMksStressLabBroker` detecta `spreadMultiplier>1 && baselineSpreadPoints==0` (o multiplicador vira no-op silencioso em `SampleSlipPoints`), emite `Print` de WARN uma vez e expõe `SpreadStressInert()` — simétrico ao argumento da ADR-030 contra o requote inerte.

### Added
- **E4.2 — rede de teste do eixo 3 em moeda (2026-07-21).** 4 testes em `Test_CMksTradeJournal.mq5` (comissão somada + `NetPnLCurrency`; **o `[M1]/[M8]`: dois journals iguais exceto a comissão → net em moeda DIFERENTE** — o sintoma do eixo 3; win+loss em moeda; sem-conversão → `*Currency=0` mas comissão ainda somada) + 3 em `Test_CMksStressLabReport.mq5` (report surfacea `netPnLCurrency`/`totalCommission`/`hasCurrency`+json; sem-conversão → `hasCurrency=false`; flag de spread inerte do E4.3). Todos **FORA** do grafo de includes do `ColorReversal` — verificados por **compile dirigido dos testes** (não `compile-all`, para não recarregar a captura viva anexada). **MT5-verificado (2026-07-21): `Test_CMksTradeJournal` 49/49 assertions (21 tests), `Test_CMksStressLabReport` 42/42 assertions (8 tests), 0 failed** ✅ (a tabela do `PrintComparison` mostrou as colunas novas `NetPnLccy`/`Comm`; o WARN de spread inerte do E4.3 imprimiu como esperado).

### Fixed
- **E3.2/ADR-033 — corrige o deadlock do soft-recovery do RenkoBuilder em rampa monotônica (2026-07-21).** O detector de gap estrutural (código 105) media `|mid − m_kFirstMid| ≤ S` contra um **primeiro-mid congelado**, não a dispersão da janela. Numa reabertura direcional sustentada (o caso mais plausível de segunda, contemplado pela ADR-008), os mids são próximos **entre si** mas se afastam progressivamente da âncora fixa → `gapDetected` ficava `false` para sempre → **travamento permanente em 102** (o exato cenário que o recovery deveria eliminar; só "salto+patamar" funcionava, "salto+tendência" não). **Fix (ADR-033):** âncora **DESLIZANTE** — `m_kFirstMid` → `m_kPrevMid` (mid da rejeição anterior); gap detectado quando o passo tick-a-tick `|mid − m_kPrevMid| ≤ S` e `run ≥ kRecoverAfter`. Rampa (passos pequenos) recupera no K-ésimo; platô continua recuperando; a deslizante re-checa a cada rejeição e **nunca trava** (uma flag "todos os passos tight" re-travaria num gap sustentado). Determinismo preservado (função pura do stream) — provado pelo teste de paridade duplo-run.
  - **E3.3 — reset de estado pós-reanchor:** o reanchor agora zera `m_lastDirection` para o inerte do construtor (`MKS_BRICK_BULL`), tirando a direção stale de `GetFormingBrick()` (simetria com o pós-init `hasFirstBrick=false`).
  - **Blindagem do invariante (achado da revisão adversarial):** o run de rejeições também é zerado no early-return `mid==lastClose` do primeiro-brick (antes só a escada resetava) — senão duas janelas de rejeição separadas por um tick exatamente no `lastClose` emendariam num recovery espúrio. Ultra-raro (igualdade exata de float num gap-run), mas o comentário afirmava "qualquer tick aceito" e agora é verdade; comentário do reset também precisado (os early-returns pré-escada não coexistem com um run ativo).
  - **Sync doc↔código:** comentários do builder corrigidos (não mais "variância ≤ S"/"primeiro mid"); a nota 2026-05-26 da ADR-011 fica preservada (§6) e refinada pela ADR-033.

### Added
- **E3.1/E3.2 — rede de teste do soft-recovery (código 105), antes com cobertura ZERO `[H2]` (2026-07-21).** 7 testes determinísticos em `Test_CMksRenkoBuilder.mq5` (classic S=10, K=3, `kRecoverAfter=3` para streams compactos): (1) **rampa monotônica recupera** — o teste do bug, falha no código antigo, passa após o fix; (2) gap-platô → exatamente 1× 105, reanchor correto, próximo movimento define direção (sem reversão); (3) spike isolado → fica 102, sem 105 (tick normal seguinte zera o run); (4) tick aceito no meio do run zera o contador (4 rejeições, máx 2 consecutivas → sem recovery); (5) **paridade duplo-run pós-recovery** — 2 builders no mesmo stream com um recovery → bricks byte-a-byte idênticos incl. `triggerPrice`/`triggerTickId` + estado final; (6) direção do forming volta ao inerte pós-reanchor (E3.3); (7) tick `mid==lastClose` no meio de um run zera o run (blindagem do invariante). Compila **0/0**; **MT5-verificado (2026-07-21): `Test_CMksRenkoBuilder` 497/497 assertions, 29 tests, 0 failed** ✅.

### Changed
- **ADR-032 — SL da estratégia em BRICKS (broker-agnóstico), não em pontos (2026-07-21).** `ColorReversal.mq5` e `DecisionReplayer.mq5` trocam o input `InpSlPoints` (pontos do símbolo — acoplado ao `digits` do broker) por **`InpSlBricks`** (default `10.0`). O composition root converte `slPoints = InpSlBricks·(brickSize/Point())`; como a distância em preço = `slPoints·Point() = InpSlBricks·brickSize` (USD), o SL é o MESMO número de bricks em qualquer `digits` (2 ou 3) — **auto-configura ao trocar de corretora**. Origem: a revisão de 2026-07-21 achou que `InpSlPoints=3000` valia **1 brick** no XAUUSDm digits=3 (`Point()=0.001`), não os "10 bricks" que o comentário do código e o §7 do checkpoint afirmavam — eixo 1 do V5-POSTMORTEM (espaço de preço mal-definido) num parâmetro de risco. **Mudança de comportamento consciente (aprovada pelo dono):** no broker digits=3 atual o SL efetivo vai de 1 brick (3 USD) para 10 bricks (30 USD). Contrato da estratégia/`CMksDecisionRunner`/ordem **intocado** (segue em pontos) — só o input do operador virou broker-agnóstico; a conversão simétrica nos dois composition roots preserva o determinismo do E2 (runner↔runner: mesmo `InpSlBricks`+`Point()` → mesmo `slPoints`). O guard M12 passa a comparar o `slPoints` derivado (reduz-se a `InpSlBricks < InpMinSlBricks` no espaço de bricks). Estratégia, runner e testes intocados. Compila **0/0 (47 arquivos)**. Checkpoint 2026-07-21 §7 reconciliado (a captura longa usa `InpSlBricks=10`).
- **E2.4 — ColorReversal troca `CMksMt5Clock`→`CMksFeedClock` no snapshot de conta (2026-07-21).** A fronteira de dia UTC do `CMksAccountSnapshot` (que rola a baseline de perda diária) passa a derivar do `timeMsc` do tick, não de `TimeCurrent()` — tira o leak de wall-clock da lógica de decisão (§1.9) e alinha o *tipo* de clock com o replay/`DecisionReplayer` (`CMksFeedClock` push-via-SetNow ≡ `CMksReplayClock` pull-do-feed). Alimentado por `SetNow(t.timeMsc)` a cada tick admitido no `OnTick`; o feed clock é semeado com o tick-âncora antes do `snapshot.Init()` (senão `NowMsc=0`→baseline no epoch; se não houver tick no attach, o 1º Send auto-corrige via rollover — degrada com graça). **Revisado adversarialmente na camada de risco: 0 defeitos.** Comportamento de risco em live: (i) o rollover de meia-noite é **igual-ou-MAIS protetor** (`TimeCurrent` já era tick-driven; feed-time rola igual-ou-depois → perda diária bloqueia igual-ou-mais no dia novo; gap de ticks não afrouxa — sem tick = sem Send); (ii) cadência do gate inalterada (só no Send, agora com feed-time); (iii) drawdown gate não lê clock → intacto; (iv) warmup não contamina a baseline.
  - **Lacuna registrada honestamente (pré-existente, NÃO regressão do E2.4, mas no alvo do §1.9 que ele invoca):** trocar o clock igualou o *tipo*, não a **cadência** do `snapshot.Update()`. O runner/golden atualiza o snapshot **por tick** (captura picos de equity intermediários → drawdown maior → bloqueia mais cedo); o live só atualiza **no Send**. Logo o gate de drawdown do live é **menos protetor que o golden**, e o decision journal do `.mkstick` pode não reproduzir a decisão Por-Conta do live. Fechar exige uma decisão de design consciente (Update per-tick no live, ou tirar o per-tick do runner) — deixada em aberto para o dono. **PENDENTE no MT5:** o critério do E2.4 (sessão cruzando meia-noite UTC → 0 diff run↔run no journal) só se verifica com uma captura real que cruze a meia-noite.

### Added
- **E2.1 (cauda) — ColorReversal grava o `.mkstick` exato do feed live (2026-07-21).** Fecha o lado da CAPTURA da paridade de decisão: o EA de produção passa a poder gravar o feed live que ele mesmo consome, para o `DecisionReplayer` replayar depois. **Aditivo e default-off** — revisado adversarialmente: com `InpRecordMkstick=false` o comportamento de trading é byte-idêntico ao de antes (builder/estratégia/risco/broker intocados; todos os sites do writer são NULL-guarded).
  - `Experts/ColorReversal.mq5` — input `InpRecordMkstick` (default false) + `CMksTickFileWriter` opcional. Grava o `MksTick` EXATO do loop do `OnTick` (após `cursor.Admit` + filtro de lixo, imediatamente antes de `IngestOne`) — o mesmo tick que alimenta o builder. **NÃO grava o fill histórico** (`RunHistoricalFill`→`IngestOne` é fonte diferente, `CopyTicksRange`, que não pode entrar num `.mkstick` de paridade). Proveniência idêntica à do `TickRecorder` Service (broker/account/symbol/digits/tickSize/point/contractSize) → o `DecisionReplayer` abre via `CMksFileTickSource`. Checkpoint no ciclo de 60s (tolerância a crash), Close no `OnDeinit` (patcheia tickCount/times).
  - **Duas guardas anti-footgun:** (a) **best-effort** — falha de `WriteTick` mid-sessão desativa a gravação + WARN, mas **NUNCA para o trading** (EA de dinheiro não morre por I/O de captura); falha ao *abrir* = `INIT_FAILED` (o operador pediu a captura). (b) **fail-fast fillDays=0** (puxa o guard do E2.3): `InpRecordMkstick=true` exige `InpHistoricalFillDays=0` — senão o replay (builder fresco) não reproduz as decisões (estado inicial veio do warmup, ausente do `.mkstick`); `!= 0` cobre até fillDays negativo. Live-only.
  - Compila **0/0 (47 arquivos)**. **PENDENTE no MT5 (dono):** rodar o ColorReversal em demo com `InpRecordMkstick=true`+`InpHistoricalFillDays=0`, depois replayar o `.mkstick` gerado pelo `DecisionReplayer` (2× → journal byte-a-byte via `verify-parity -JournalA -JournalB`). Isso vira o **golden real-tick do E2.2**.
  - **Fragilidade pré-existente registrada (fora do escopo, compartilhada com o TickRecorder):** um `WriteTick` que falha no meio de um record deixa bytes órfãos e o `CMksTickFileReader` rejeita o arquivo INTEIRO (`fileSize != 256 + tickCount·64`). Item de hardening futuro ("recuperar até o último record íntegro"), não introduzido por esta mudança.

### Added
- **E2.1 (slice-1) — replay da decisão + diff do decision journal no verify-parity (2026-07-21).** Começa a estender a prova de paridade dos bricks para a DECISÃO. Duas peças, no reframe honesto (sim↔sim = determinismo, NÃO live-broker↔replay):
  - `tools/verify-parity.ps1` — **novo modo de diff do decision journal** (`-JournalA`/`-JournalB`), o sucessor do log-diff stub da seção 4 (que filtrava linhas de brick do `.log`). Compara o oráculo de paridade da DECISÃO: descarta as linhas `#` (header do bundle pinado — proveniência que pode diferir e é assertada à parte no E2.3 — e o rodapé `# total=`), compara cabeçalho de colunas + linhas de dados. Semântica **runner↔runner (determinismo)** ou **runner↔golden (regressão)**. Herda a guarda anti-vácuo do M18/E0.4: 0 decisões em AMBOS = falha (exit 2), nunca "OK". Os pares `.mksbk`/journal/log ficaram independentes (`-LiveMksbk`/`-ReplayMksbk` deixaram de ser mandatórios) — o caminho `.mksbk` validado foi só envolvido num `if`, intacto. **Testado localmente (pwsh): journals idênticos com header diferente→0; campo divergente→2 com o diff; vácuo→2; contagens divergem→2; sem par/arquivo ausente→3; regressão `.mksbk` idênticos→0/divergente→1; combinado mksbk-OK+journal-divergente→2.**
  - `Experts/MKS-ULTIMATE/DecisionReplayer.mq5` — **novo EA** (composition root dedicado; o dono escolheu EA separado em vez de estender o `Replayer.mq5` validado — risco zero ao caminho `.mksbk`). Abre um `.mkstick` (single ou multi-file, mesmos helpers de proveniência do Replayer) e dirige o `CMksDecisionRunner` por tick, emitindo o decision journal determinístico. Config pinada = o bundle versionado (brick/SL/lote/custo/risco). **Fail-fast anti-falha-silenciosa:** como os numéricos do símbolo (point/tickSize/tickValue) são load-bearing aqui (sizer/piso-de-SL/conta) — diferente do Replayer, onde o símbolo é só metadado — recusa anexar (`INIT_FAILED`+`Alert`) se forem 0 (símbolo indisponível → sizing/risco degenerados), apontando a causa-raiz. Compila **0/0 (47 arquivos)**; revisão adversária focada (lifecycle/wiring/determinismo/fidelidade ao template) — 0 defeitos reais, com o fail-fast do símbolo aplicado a partir dela.
  - **PENDENTE no MT5 (dono):** rodar o `DecisionReplayer` sobre um `.mkstick` real e verificar o journal (e um 2º run byte-a-byte via `verify-parity -JournalA -JournalB`). A **captura do `.mkstick` real** (para o golden E2.2) e a gravação do `.mkstick` exato + swap de clock no `ColorReversal` (cauda do E2.1 / E2.4) seguem dependendo de você no MT5.

### Added
- **E2.0 (fatia 2) — o grafo DDR fecha: `CMksJournalingBroker` + `CMksDecisionRunner` + `Test_DriveOrder` (2026-07-21).** Completa os três pendentes da fatia anterior (o `# Pendente` do bloco E2.0 abaixo). Com isto o núcleo determinístico da camada de decisão está montável e dirigível de ponta a ponta; falta só a execução no MT5 (o dono roda) e as fatias E2.1–E2.4 + o golden real-tick (E2.2).
  - `Core/Trade/CMksJournalingBroker.mqh` — **novo**: decorator de `IBroker`, o broker MAIS EXTERNO que a estratégia chama. Em `Send`/`Close` delega ao inner (o gated broker), grava a DECISÃO no `CMksDecisionJournal` (`RecordSend`/`RecordClose` com o `brokerRetcode` real — inclui o REJECTED do gate, que o inner nunca vê) e alimenta o `CMksSimAccount` só no fill (`OnOpen`/`OnClose` com os números reais do resultado). Non-owning. Convenção: o journal grava a INTENÇÃO (`request.lots` — função pura da decisão do sizer, estável no diff), a conta usa a REALIDADE (`filledLots`). Auto-close de SL/TP NÃO passa por aqui (é drenado pelo runner) → sem alimentação dupla da conta.
  - `Strategy/Runner/CMksDecisionRunner.mqh` — **novo**: dono do grafo DDR inteiro (monta CostModel→SimBroker→SimAccount/book/FeedClock/Snapshot→RiskManager+SL floor→RiskGatedBroker→Journal→JournalingBroker→ColorReversalStrategy→contextSink+MultiSink→RenkoBuilder) e expõe UM helper de ordem-por-tick (`OnTick`), ÚNICO e anti-bifurcação: `clock.SetNow → account.SetMid → simBroker.OnTick → drena PollAutoCloses (journal+conta) → builder.IngestTick (contextSink ANTES da estratégia no MultiSink) → snapshot.Update`. Reside em `Strategy/` (não `Core/`) por ser acoplado ao `CMksColorReversalStrategy` — não inverter Core→Strategy. Config pinada (`MksDecisionRunnerConfig`) = a unidade versionada do golden. Header do journal capturado do 1º tick válido (âncora seedMid/seedTickSeq do feed), zero wall-clock. `CMksDecisionContextSink` (sink que carimba `SetBrickContext(brickIdx, triggerTickId)` antes da estratégia). O Replayer (E2.1) e o golden headless (E2.2) instanciam o MESMO runner — a prova de determinismo run↔golden depende de haver um só grafo.
  - `Scripts/.../Tests/Test_CMksDecisionRunner.mq5` — **novo** (4 tests): (1) determinismo byte-a-byte de dois runs do mesmo feed+config em dois cenários — flips (SEND+CLOSE via broker: 4 sends/3 closes/7 eventos) e SL hit (AUTOCLOSE via drain: 3 auto-closes/4 sends/3 auto-detach); (2) **`Test_DriveOrder`** — prova que a ordem `simBroker.OnTick` ANTES de `builder.IngestTick` é LOAD-BEARING (parte A: o fill do Send depende do último OnTick, 2010 vs 2000 stale; parte B: o runner abre o SELL no mid do tick de gatilho ATUAL 2003, não no anterior 2006 — inverter a ordem interna quebra a assertion).
  - **Reframe honesto mantido:** o runner prova DETERMINISMO da camada de decisão (sim↔sim/golden), NÃO fidelidade ao broker real. Nada aqui autoriza dizer "H4 fechado".
  - Compila **0/0 (46 arquivos)** via `compile-all.ps1`. Revisão adversária multi-agente (5 lentes: determinismo, contrato-de-ordem, paridade/regras, trace-dos-testes, ciclo-de-vida MQL5) — **0 achados reais**, com trace independente confirmando as contagens dos testes. **Rodado no MT5: `Test_CMksDecisionRunner` 36/36 assertions, 4 tests, 0 failed** ✅ (determinismo byte-a-byte flips + SL-hit, `Test_DriveOrder` contrato de ordem). E os 3 pendentes da fatia 1 verificados na mesma sessão: `Test_CMksSimAccount` 27/27 (10 tests), `Test_CMksSimPositionBook` 17/17 (5 tests), `Test_CMksDecisionJournal` 10/10 (2 tests), 0 failed — **toda a fundação E2.0 está MT5-verificada.**

### Added
- **E2.0 (fatia) — fundação determinística da paridade de DECISÃO (design DDR via workflow, 2026-07-19).** Início do gate central E2. Um workflow de design (3 arquiteturas → refutação adversária → síntese) estabeleceu a arquitetura **MKS Deterministic Decision Runner (DDR)** e um **reframe honesto** aprovado pelo dono: paridade de decisão bit-a-bit entre a estratégia REAL (broker real) e o replay (sim) é **estruturalmente impossível** (o SL real dispara em tick/preço que o sim não reproduz); então o E2 prova (1) paridade feed→brick live↔replay, (2) **determinismo** da camada de decisão sim↔sim/golden, e **nomeia** o gap sim↔real como risco do StressLab (eixo 3). Fica proibido em doc futuro afirmar "H4 fechado". Esta fatia entrega as peças-folha, cada uma testada em isolamento (testes antes de EA):
  - `Core/Account/CMksSimAccount.mqh` — **novo**: `IAccount` com equity = startBalance + P&L realizado (closes/auto-closes − comissão) + MTM não-realizado das posições sim ao mid, na convenção de dinheiro do sizer (`(priceDiff/tickSize)·tickValue·lots·sinal`). Fecha o eixo 3 no runner: sem ela o breaker de conta (a camada onde o V5 quebrou) ficaria inerte (a conta fake devolve 10000 fixo). Swap OFF em v1 (decisão do dono: swap-free agora, marcado no journal; accrual fica para o E4).
  - `Core/Position/CMksSimPositionBook.mqh` — **novo**: `IPositionBook` lastreado no `CMksSimulatedBroker`, com `IsOpen(desconhecido)=false` igual ao book REAL (não a inversão do mock, que mascararia divergência no auto-detach).
  - `Core/Clock/CMksFeedClock.mqh` — **novo**: `IClock` cujo "agora" é o `timeMsc` do último tick admitido (análogo live do `ReplayClock`) — base do E2.4 (fronteira de dia UTC derivada do feed nos dois ambientes).
  - `Core/Broker/CMksSimulatedBroker.mqh` — carimba `triggerTickSeq` (seq do tick que cruzou SL/TP) no `MksSimAutoCloseEvent` + enumerador `OpenPositionByIndex` (para o SimPositionBook). Sem mudança de comportamento.
  - `Core/Trade/CMksDecisionJournal.mqh` — **novo**: journal TSV determinístico das decisões (SEND/CLOSE/AUTOCLOSE/REJECT), o oráculo de paridade da camada de decisão. Colunas `ord/ev/status/brickIdx/triggerTickSeq/side/posOrdinal/lots/slPoints/tpPoints/retcode`; `posOrdinal` normalizado (remove o ticket do broker), tempo do feed, zero wall-clock/RNG. Header `#` com o bundle pinado (cost model + conta + config + âncora), ignorado no diff.
  - Testes: `Test_CMksSimAccount.mq5` (10), `Test_CMksSimPositionBook.mq5` (5 + FeedClock), `Test_CMksDecisionJournal.mq5` (2: determinismo byte-a-byte + normalização de posOrdinal). Compilam 0/0 (45 arquivos).
  - **Pendente nesta fatia:** `CMksJournalingBroker` (decorator que grava journal + alimenta a conta) + `CMksDecisionRunner` (helper de ordem-por-tick, anti-bifurcação) + `Test_DecisionRunner/DriveOrder`. Handoff completo em `docs/CHECKPOINT-2026-07-20-sessao.md`.

### Added
- **Resgate do doc de auditoria detalhada de 25/05 antes de aposentar a branch zumbi (2026-07-19).** A branch remota `claude/check-ultimate-access-5kshn` (rodada automática de 25/05, congelada em `45c3a67`, ~82k linhas atrás de main) tinha 1 commit único: `docs/AUDITORIA-2026-05-25.md` (288 linhas, auditoria arquivo-por-arquivo) que nunca subira para main — só o resumo (`CHECKPOINT-2026-05-25-audit.md`) estava versionado. Doc resgatado para main com nota de "documento histórico" (achados já tratados via ADRs/E1/E0) + referência cruzada no índice `CHECKPOINTS.md`. Branch zumbi deletada após o resgate (nada perdido).

### Removed
- **E0.7 — limpeza da auditoria 2026-07-19 (código morto e redundante).**
  - `MQL5/Scripts/MKS-ULTIMATE/ValidateRenkoBuilder.mq5` — **deletado** (0 asserts, nem inclui Asserts.mqh; os 6 cenários têm asserts reais em `Test_CMksRenkoBuilder` — puro falso senso de cobertura, M6).
  - `reference/V5/MKS-Dashboard.mq5`, `MKS-Dashboard2.mq5` — **deletados** (0 bytes); `reference/V5/V5-Dashboard2.mq5` — **deletado** (byte-idêntico a `V5-Dashboard.mq5`, que fica).
  - `DumpMksTick.mq5` — removido `prevSeq` (atribuído, nunca lido) + **corrigido div-por-zero** quando `InpPrintLastN=0` (crashava no 1º tick em `% lastN`).
  - `ColorReversal.mq5` — removido `InpComment` (input morto; a estratégia hardcoda `"ColorReversal"`).
  - **Não deletado (flag ao dono):** a branch remota `origin/claude/check-ultimate-access-5kshn` tem um commit ÚNICO não mergeado (`73a93f6`, "docs: audit 2026-05-25"); deletar é outward-facing e perderia esse commit — decisão do dono. `g_nextBarTime`/`m_magic`/`g_clock` (cosméticos) diferidos.

### Added
- **E0.6 — segurança operacional pós-incidente das junctions (2026-07-19).**
  - `docs/CHEATSHEET.md §9.6` — runbook "Desinstalar/atualizar o MT5 com segurança": remover as 5 junctions com `rmdir` ANTES de desinstalar (a desinstalação atravessa junction viva e apaga o alvo no repo). `§9.7` — backup dos dados de captura.
  - `tools/backup-captures.ps1` — **novo**: robocopy dos `.mkstick`/`.mksbk`/`.log` de `MQL5\Files\MKS-ULTIMATE\` (gitignorado, dentro do terminal) para fora da árvore do terminal (default `C:\dev\MKS-DATA`, `-Dest` configurável). Não espelha-deletando (preserva capturas antigas). Sintaxe validada; não agendado (Task Scheduler fica a critério do dono).

### Fixed
- **Testes de auto-trigger do SimulatedBroker assumiam `point=1.0` sem setá-lo (achado novo, exposto ao rodar a suíte no MT5 em 2026-07-19).** `Test_AutoTriggerSlBuy`, `Test_AutoTriggerStaysOpenWhenNoHit` e `Test_AutoTriggerHalfSpreadBidProxy` usavam aritmética de preço inteiro (SL 10 pts = 1990, fill+halfSpread = 2005), mas o `CMksFakeSymbol` tem `point=0.01` por default (XAU) desde a criação do mock — deixando SL/TP 100× mais perto e disparando cedo (SL absoluto 1999.95 em vez de 1995). Falhas **pré-existentes** (não do E0.3 — o backstop novo é inerte com `minSlPoints=0`, e a suíte não rodava no MT5 há tempo), reveladas pela verificação empírica. Fix: os testes de auto-trigger passam a chamar `sym.SetPoint(1.0)` explicitamente, casando a aritmética com a intenção. É a classe "cobertura fantasma" que o M20 combate — um instrumento de confiança que estava vermelho e ninguém via.
  - `Test_CMksSimulatedBroker.mq5` — `SetPoint(1.0)` nos 5 testes de auto-trigger (3 corrigem falha + 2 por consistência). A verdade da produção (`slPrice = fillPrice − slPoints·point`) já estava correta; o defeito era só nos testes. **Rodado no MT5: 83/83, 20 tests, 0 failed** ✅ (era 76/83, 3 failed).

### Fixed
- **E0.3 — piso de SL mínimo ancorado em bricks, fail-closed, fonte única no RiskManager (achado M12 da auditoria 2026-07-19).** O gate de SL do E1.1 usava `minSlPoints = StopsLevel()`, que é **0 na Exness XAU** → o gate virava NO-OP e um SL curto passava, levando `INVALID_STOPS (10016)` em toda ordem live enquanto o backtest preenchia (divergência eixo 2). E o `CMksSimulatedBroker` aceitava **qualquer** SL — o mecanismo que fazia o backtest mentir. **Design escolhido via workflow de projeto** (3 abordagens reprovadas adversarialmente → híbrido; 2 decisões de apetite de risco confirmadas pelo dono: piso de 1 brick, termo dinâmico diferido ao E2):
  - O piso efetivo passa a ser `max(InpMinSlFloorPts, InpMinSlBricks·brickSizePts)`, computado **dentro** do `CMksRiskManager` (fonte única, via `SetSlFloorSource`) — função pura de config, **idêntica em live/tester/replay** (paridade da decisão). Com `InpMinSlBricks>=1` o piso é sempre > 0 → o gate fica **sempre ativo**, matando o NO-OP do M12 estruturalmente.
  - **StopsLevel FORA do número de runtime** (tester/replay podem modelá-lo diferente do live → env-leak que quebraria a paridade da decisão): entra só como **fail-fast de anexação** no OnInit (recusa anexar se o piso configurado ficar abaixo do stops level do broker, ou se `InpSlPoints < piso` — este último é o cenário exato do M12, agora recusado *antes* de qualquer ordem). **FreezeLevel continua fora** (é do Modify/trailing, E5).
  - **Backstop** no `CMksSimulatedBroker` (param `minSlPoints=0.0`, default off preserva ~20 testes): rejeita SL abaixo do piso com o **mesmo código 410** do gate (registro idêntico bt/live). Não-autoritativo — a decisão de paridade é do gate; no runner de replay do E2 o sim é envolvido num `CMksRiskGatedBroker` com o mesmo RiskManager.
  - **Diferido ao E2** (honestidade registrada): o termo spread-aware por-tick, que fecha a cauda residual "spread instantâneo > piso"; e o backstop autoritativo via `CMksRiskGatedBroker` no runner de replay (que ainda não existe). Nenhum piso estático zera o 10016 que o servidor levanta no micro-instante do OrderSend — limite estrutural, patológico num Renko.
  - Arquivos: `Core/Risk/CMksRiskManager.mqh` (membros + `SetSlFloorSource` + `EffectiveMinSlPoints` + Validate fail-closed + gate por `EffectiveMinSlPoints`), `Core/Broker/CMksSimulatedBroker.mqh` (backstop), `Experts/ColorReversal.mq5` (inputs `InpMinSlBricks=1`/`InpMinSlFloorPts=0.0` + fail-fast de anexação + `SetSlFloorSource`), `Core/Types/Error.mqh` (comentário do 410). **11 testes novos** (gate ativo com StopsLevel=0; imunidade ao StopsLevel/env-leak; determinismo cross-instância; wiring fail-closed; belt-max; backstop do sim rejeita/aceita/off-by-default). **Rodado no MT5: Test_CMksRiskManager 119/119 (70 tests), Test_CMksSimulatedBroker 83/83 (20 tests), 0 failed** ✅. Detalhes e trade-offs em `docs/CHECKPOINT-2026-07-19-auditoria.md`.

### Changed
- **E0.5 (slice) — sync de status doc↔código (achado M24 da auditoria 2026-07-19).** Corrige a "fila de trabalho que mente": (a) `ROADMAP-CORE-HARDENING.md` Fase E1 estava "Não iniciada" com E1.1–E1.4 commitados → marcada Concluída, com nota registrando o desvio (gate no Risk, não no broker) e a extensão pela Fase E0/M12; (b) `ROADMAP.md` Fase 9 narrava os 3 furos do StressLab como pendentes → marcados fechados pela ADR-030; (c) `ARCHITECTURE.md §4` afirmava "nenhuma decisão pendente" → lista a ADR-031 (não escrita) e o termo spread-aware dinâmico diferido ao E2. A skill `/status` lê a §4 e agora reporta as pendências reais.
- **E0.5 (slice) — `.claude/settings.local.json` deixa de ser versionado (achado M25 da auditoria 2026-07-19).** O arquivo acumula permissões por-máquina (paths de `%TEMP%`) e aparecia como working-tree modificado toda sessão, nas duas máquinas. `git rm --cached` + linha no `.gitignore`; a allowlist compartilhada (`settings.json`) segue versionada.

### Fixed
- **E0.4 (slice) — TestRunner deixa de aprovar suíte vazia / teste sem assertion (achados M20/N2 da auditoria 2026-07-19).** `End()` contava como "passou" um teste que rodou **zero assertions** (refactor que apaga asserts, `MKS_RUN` de função vazia, early-return), e `Summary()` só alertava em `failedAssertions>0` — deixando "suíte verde sem cobertura" passar calada. Agora: teste sem assertion é contado à parte (`m_emptyTests`, linha `EMPTY [nome]` no journal) e **não** conta como passado; `Summary()` alerta em qualquer condição de falha — suíte vazia (`totalTests==0`), assertions/tests falhados, **ou** testes-fantasma. Varredura estática confirmou que nenhuma das 26 suítes tem teste sem assertion → o guard não gera falso-positivo; suítes limpas seguem "N/N (0 failed)". **N2:** o meta-teste `Test_MksTestFramework` deixava de chamar `runner.Summary()` sobre estado poluído por uma falha sintética (Phase 5) → não mais dispara "1 FAILED" espúrio; e seu helper `TestSmokeStringification` ganhou uma assertion legítima.
  - `Core/Testing/TestRunner.mqh` (guard de teste-vazio + Summary com alerta abrangente + getter `EmptyTests`), `Test_MksTestFramework.mq5`. **Rodado no MT5: meta 6/6 checks, sem o "1 FAILED" espúrio** ✅.
- **E0.4 (slice) — verify-parity deixa de passar no vácuo no log-diff (achado M18 da auditoria 2026-07-19).** Se o padrão de decisão não casasse NENHUMA linha em ambos os logs (chave do JSON renomeada, sessão sem bricks, filtro desatualizado), `liveNorm.Count == replayNorm.Count == 0`, a checagem de contagem passava e o script reportava "logs IDÊNTICOS (0 decisões)" com exit 0 — falso-OK que não provava paridade nenhuma. Agora "0 linhas em AMBOS" é falha (exit 2) com diagnóstico. (A comparação mais profunda via audit TSV simétrico fica para o E2.1, que expande o verify-parity para o stream de decisão.)
  - `tools/verify-parity.ps1` — guarda de passe-vácuo; sintaxe revalidada via parser.
- **E0.4 (slice) — compile tools deixam de reportar falso-OK com log vazio (achado M23 da auditoria 2026-07-19).** `tools/compile-all.ps1` e `tools/watch-compile.ps1` contavam 0 erros/0 warnings quando o MetaEditor não rodava (não encontrado, crash, GUI concorrente travando o arquivo) e o log saía vazio → reportavam "OK"/"TODOS COMPILAM LIMPOS" (exit 0). É a mesma classe do M9: ferramenta de confiança que dá falsa confiança. Agora log vazio/ausente é tratado como **"SEM CONFIRMAÇÃO" → falha** (compile-all: linha própria no summary + exit 1; watcher: linha `?` vermelha), nunca como sucesso. `compile-all` passou a capturar o exit code do MetaEditor (`-PassThru`) para diagnóstico. Um compile real sempre escreve a linha de resultado no log — a ausência dela é o sinal.
  - `tools/compile-all.ps1`, `tools/watch-compile.ps1` — flag `unknown`/log-vazio; verificado rodando o próprio compile-all (43 arquivos, tree limpa).

### Fixed
- **E0.2 — posição órfã adotada no restart + flatten no halt de stream (achados M10/M19 da auditoria 2026-07-19).** (a) **M10:** re-anexar o EA com posição aberta (recompile, reboot de VPS) deixava a órfã sem gestão — o estado da estratégia nascia vazio, o flip não a fechava, e com `maxOpenPositions=1` o book a contava e todo Send novo levava 405: EA mudo até o SL de 3000 pts bater. É o invariante 5 do V5-POSTMORTEM ("reconstrução de estado é completa ou não acontece") aplicado a posições. Agora o OnInit reconcilia: 1 órfã → **adotada** (flip fecha, auto-detach monitora, `Alert` informa); 2+ órfãs → **fail-fast** com `Alert` (estado que a estratégia de 1 posição não sabe reconstruir — mesmo padrão da guarda ADR-029). No tester o book está vazio no OnInit — mesmo caminho, no-op (sem bifurcação). (b) **M19:** halt do stream (104) com posição aberta deixava a posição sem gestão, sem alerta, exposta até o SL. Agora: **flatten + `Alert`** (EA cego não segura posição); se o Close falhar, `Alert` exige intervenção manual e o vínculo é mantido (auto-detach/operador seguem enxergando).
  - `Core/Interfaces/IPositionBook.mqh` — contrato novo `PositionAt(index, id&, side&, lots&)` (snapshot enumerável do escopo symbol+magic); implementado em `CMksMt5PositionBook` (O(N), só OnInit) e `CMksFakePositionBook` (`AddPosition` + `PositionAt`).
  - `Strategy/CMksColorReversalStrategy.mqh` — `AdoptPosition(id, side, lots)` (religa o vínculo como se a instância tivesse aberto) e `FlattenForSafety(reason)` (fecha fora do fluxo de flip; **mantém** o vínculo em falha, diferente do `CloseCurrentIfAny`).
  - `ColorReversal.mq5` — passo 12.5 (reconciliação de órfã no OnInit) + `Alert`/flatten no ramo 104 do `IngestOne` (sempre alerta no halt, com ou sem posição).
  - `Test_CMksColorReversalStrategy.mq5` — **6 testes novos**: adoção restaura vínculo (via book fake `PositionAt`), flip fecha a adotada, auto-detach da adotada, flatten fecha+limpa, flatten falho mantém vínculo, flatten sem posição é no-op. **Rodado no MT5: 89/89 assertions, 20 tests, 0 failed** ✅ (14→20 tests).

### Fixed
- **E0.1 — dedup por contagem na fronteira de ms: ticks same-ms preservados (achado H5 da auditoria 2026-07-19).** O dedup antigo (`time_msc <= último visto`, com o cursor avançando por tick) descartava TODOS os ticks do mesmo milissegundo exceto o primeiro — dentro do batch, não só na fronteira do `CopyTicks`. Em burst (XAU/notícia), o brick podia fechar em tick diferente do que o mercado imprimiu (`triggerPrice`/`triggerTickId` divergentes); o `.mkstick` do TickRecorder **não era captura crua** (violava ADR-012 §1); e o fill histórico (`CopyTicksRange`, feed completo) alimentava o mesmo builder com granularidade diferente do live (eixo 2 do V5 via dados). Invisível ao `verify-parity` porque Producer e Recorder decimavam identicamente. **Pré-requisito do golden fixture E2.2** (gravado com o dedup antigo, congelaria o defeito como "correto").
  - `MQL5/Include/MKS-ULTIMATE/Core/Data/TickBatchCursor.mqh` — **novo**: `MksTickBatchCursor` (POD), dedup por CONTAGEM de ticks já vistos no ms da fronteira (`FromMsc`/`BeginBatch`/`Admit`); repetições do batch anterior são puladas, ticks novos do mesmo ms são admitidos. `ResetAfter(anchor)` preserva a semântica de âncora pré-H5 (ms da âncora inteiro consumido — limitação deliberada e documentada, restrita ao startup).
  - `Producer.mq5`, `ColorReversal.mq5`, `Services/TickRecorder.mq5` — os três pumps migrados para o MESMO cursor (anti-eixo-2 por construção); no ColorReversal, lixo estrutural agora também conta como visto (antes não avançava a âncora). `ticksSkippedDup` do Recorder passa a contar só repetições reais de fronteira.
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_MksTickBatchCursor.mq5` — **novo**: 10 testes — regressão H5 (burst same-ms 4/4 admitidos), overlap de fronteira entre batches, refetch idêntico, lixo contado como visto, âncora, tick atrasado, estado virgem, determinismo dual-cursor. **Rodado no MT5: 35/35 assertions, 10 tests, 0 failed** ✅.

### Added
- **Auditoria completa #2 do projeto (2026-07-19) + plano norteador.** Cinco agentes de área releram o repo integralmente contra ADRs/V5-POSTMORTEM/auditoria-2026-06-02; os 2 achados ALTOS foram verificados manualmente no código. Estado real do hardening confirmado: **E1 feito** (E1.1–E1.4, com ressalvas), **E2–E6/E8 não iniciados, E7 parcial**; os 3 furos do StressLab (Fase 9) confirmados fechados pela ADR-030. Achados novos: **H5** (dedup por `time_msc` decima o feed live no Producer/ColorReversal/TickRecorder — viola a captura crua da ADR-012, invisível ao `verify-parity`, pré-requisito de E2.2), **H6** (barras órfãs de formação, com wicks incondicionais, poluem os 6 indicadores no CS — fix barato via `tick_volume==0`), + 16 médios e ~40 baixos (famílias: estados não reconciliados — posição órfã no restart, halt com posição aberta, timeout-com-fill; instrumentos de confiança com falso-OK — TestRunner aprova suíte vazia, compile tools OK com log vazio, verify-parity passa no vácuo). Plano: **fase E0 (correções imediatas)** → E2–E5 → E7+H6/E8 → **E9 dashboard** (`CMksStatusPanel`, spec no relatório) → Fase 10.
  - `docs/CHECKPOINT-2026-07-19-auditoria.md` — **novo**: relatório completo (incidente das junctions, método, achados, status E1–E8, plano norteador, lista de eliminação).
  - `docs/CHECKPOINTS.md` — índice atualizado.
  - **Incidente operacional (2026-07-19):** desinstalação do terminal MT5 atravessou as 5 junctions e apagou `MQL5/*/MKS-ULTIMATE` no repo — recuperado integralmente via `git checkout HEAD` (capturas `.mkstick`/`.mksbk` fora do git foram perdidas). Terminal Exness reinstalado (mesmo hash `53785E09...`), 5 junctions recriadas, toolchain revalidada: **42 arquivos compilam 0 erros / 0 warnings** via `compile-all.ps1`.

### Added
- **Auditoria completa do projeto (2026-06-02).** Auditoria multi-agente das 10 dimensões (paridade, renko, custom-symbol, indicadores, broker/custo, trade/risk, estratégia/EA, testes, doc↔código, UX/processo) com leitura integral do core e verificação adversária de cada achado alto/médio. **58 achados** (4 altos, 9 médios, 45 baixos/cosméticos; 1 refutado). Veredito: core estruturalmente sólido (4 eixos do V5 fechados no código), mas **paridade mais estreita que os docs afirmam** (provada só em `fillDays=0`/stream de bricks; decisão→equity nunca replayada).
  - `docs/CHECKPOINT-2026-06-02-auditoria.md` — **novo**: relatório completo (método, veredito, achados por severidade, positivos, refutado).
  - `docs/ROADMAP-CORE-HARDENING.md` — **novo**: plano de fechamento do core em 8 fases (E1–E8), cada item rastreando um achado. **E1–E5 gate de estratégias; E7+E8 gate de indicadores.**
  - `docs/ROADMAP.md` — **Fase 9.5** (fechamento/endurecimento do core) inserida como gate entre Fase 9 e Fase 10; Fase 10 marcada bloqueada por E1–E5.
  - `docs/CHECKPOINTS.md` — índice atualizado.

### Fixed
- **E1.1 — SL validado contra o stops level do broker (achado H3 da auditoria 2026-06-02).** O default `InpSlPoints=30` causava `INVALID_STOPS` (10016) em produção (06-02), e o broker montava o SL sem nunca consultar `SYMBOL_TRADE_STOPS_LEVEL` (apesar de `ISymbol::StopsLevel()` já existir). A guarda foi colocada no **RiskManager** (gate simétrico backtest/live), não no broker — assim o backtest rejeita o MESMO SL muito-próximo que o live rejeitaria, evitando divergência (eixo 2).
  - `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — código `MKS_ERR_RISK_REJECTED_SL_BELOW_STOPS = 410` (faixa Risk).
  - `MQL5/Include/MKS-ULTIMATE/Core/Risk/CMksRiskManager.mqh` — `CMksRiskTradeParams.minSlPoints` (0 = sem checagem); `CheckOrder` rejeita `slPoints < minSlPoints` (passo 1.5, após SL-missing); `Validate` recusa `minSlPoints < 0`.
  - `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — `OnInit` lê `g_iSymbol.StopsLevel()`, popula `rtp.minSlPoints` e faz **fail-fast** com `Alert` (informando o mínimo) se `InpSlPoints` < stops level; default de `InpSlPoints` corrigido `30 → 3000` (empírico Exness XAU). `stopsLevel=0` desliga a checagem (broker-agnóstico).
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksRiskManager.mq5` — **6 testes novos** (rejeição, fronteira `==`, acima, default-off, precedência do SL-missing, `Validate` negativo).
  - **Compila 0/0** no MT5 (ColorReversal + Test_CMksRiskManager + Test_CMksRiskGatedBroker via MetaEditor64 headless). Teste rodado no MT5: **98/98 assertions, 65 tests, 0 failed** ✅.

### Added
- **Fundação de sensores integrada (ADR-025) + consolidação de branches (2026-06-02).** `feat/sensors-foundation` auditada (veredito limpo: `ISensor` observador-não-decisor, `CMksReversalRegime` classe pura, indicador de debug reusa o core; zero API proibida, não toca trading/paridade) e mergeada em `main`. `feat/indicators-bundle-1` já estava em `main`. Branches `phase9-trade-visualization`/`sensors-foundation`/`hedging-only-guard`/`indicators-bundle-1` deletadas — **`main` passa a ser a branch de integração**. Foundation-only: nenhum sensor novo / Caixote até o core fechar (E8).
  - `Core/Interfaces/ISensor.mqh`, `Core/Types/SensorState.mqh`, `Core/Sensors/Regime/CMksReversalRegime.mqh`, `Indicators/MKS-ULTIMATE/CMksReversalRegimeDebug.mq5` — **novos** (compilam 0/0 no tree mergeado). ADR-025 em `ARCHITECTURE.md` (conflito documental resolvido: mantidas ADR-025 + ADR-026..030).

### Changed
- **E1.2 — watcher cobre Experts/Services e `#include "..."` (achado M9 da auditoria).** `tools/watch-compile.ps1`: `Experts/MKS-ULTIMATE` e `Services/MKS-ULTIMATE` adicionados a `entryRoots` (grafo reverso) e `watchRoots` — salvar um `.mqh` do core agora recompila ColorReversal/Producer/Replayer/TickRecorder. `Get-IncludesFromFile` passou a resolver também includes quote-style (`#include "..."`, relativo ao arquivo) — editar `TestRunner.mqh` agora recompila os testes consumidores. Verificado em execução: startup limpo, **42 entry points / 67 headers mapeados** (antes só Scripts+Indicators). Bug de PowerShell 5.1 corrigido no caminho (`Split-Path -LiteralPath -Parent` → `[IO.Path]::GetDirectoryName`).
- **E1.3 — reconciliação doc↔código (achados M5 + drift do logger).** `docs/ARCHITECTURE.md`: Nota de esclarecimento na ADR-024 mapeando os `MKS_ERR_TICKFILE_*` (que **não existem** no código) para os `MKS_ERR_DATA_*` reais (ex.: symbol mismatch = 807, não 803; seq = 810, não 804). `docs/ROADMAP.md` Fase 8: bullet "output plugável" alinhado ao real (destino dual fixo Print+arquivo, ADR-007); "ticket" reclassificado de contexto automático para exemplo de `ctxJson` por chamada.

### Changed
- **Marcadores de trade (ADR-028) com estilo configurável (2026-05-30).** `CMksChartPainter`: setas reduzidas (width 2→1) com borda branca (halo — glyph maior atrás do colorido), linha conectora tracejada (`STYLE_DASH`) e movida para a frente dos bricks (`OBJPROP_BACK` true→false). Cores agora vêm de `MksChartPainterStyle` (POD) injetado via `SetStyle`; `ColorReversal.mq5` expõe os inputs `InpVizArrowBuy/Sell/Border` e `InpVizLineProfit/Loss`. Larguras e estilo da linha ficam como default do struct. Output puro — não toca decisão/execução (paridade preservada); compila 0/0 no MT5. Marcadores são chart-agnósticos: o estilo carrega para qualquer chart-alvo (NB: ADR-031 revertida em 2026-06-02 — o CS é **mantido+corrigido**, não migrado para fora). **⚠️ Halo/dash/z-order pendentes de confirmação visual no chart.**

### Fixed
- **Cap no runaway do bump da timeline híbrida do CS (ADR-023-A, 2026-05-29).** `brickTime` no `CMksCustomSymbolSink` agora tem teto (`realTime + maxFutureDriftSecs`, default **6h**), via a função pura `ComputeBrickTime` (testável sem Custom Symbol). Isso elimina o runaway **auto-infligido** em que o bump (`nextBarTime += 60`) empurrava a barra do CS +60s/brick num mercado >1/min sustentado.
  - `MQL5/Include/MKS-ULTIMATE/Core/Output/CMksCustomSymbolSink.mqh` — cap + `ComputeBrickTime`. Em mercado calmo nada muda; ao bater o teto numa rajada, bricks do mesmo minuto se sobrescrevem (CS amostra ≤1/min) e autocura quando o mercado desacelera.
  - **`.mksbk` e estratégia intactos** — não leem o CS (ADR-020 §1); degradação é exclusivamente visual. **`Producer` herda** (mesmo sink).
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksCustomSymbolSink_Timeline.mq5` — **novo**: rodado no MT5, **510/510 assertions, 9 tests, 0 failed** ✅ (calmo/bump/cap/autocura + regressão do runaway).
  - **⚠️ CORREÇÃO (2026-05-30) — o cap NÃO cura a morte do CS.** Análise multi-agente + forum MQL5 (360938/335324/363224) mostrou que a causa-raiz é um **bug de plataforma não-corrigido do MT5**: `CustomRatesUpdate` corrompe o container do Custom Symbol na **virada de dia do server** (apaga a série inteira; `-1`/`GetLastError()=0`/"invalid container"), **independente** de barra-no-futuro. O cap continua válido como higiene (remove um amplificador), mas o diagnóstico original "recusa barra além de ~fim-de-amanhã" era uma inferência não-confirmada. `CustomRatesReplace` e `CustomTicksAdd→OHLC` compartilham o mesmo container bugado. **Correção estrutural (em decisão): aposentar o CS do caminho de visualização** (renko brick-native via indicador/objetos no chart do símbolo real + família `IRenkoIndicator` da ADR-020-A) — ADR nova (próxima livre, **ADR-031**; ADR-023/023-A passam a candidatas a superseded para o caminho live).
  - `docs/ARCHITECTURE.md` — **Emenda ADR-023-A** + nota de correção 2026-05-30.
  - **🔄 REVERSÃO (2026-06-02) — o veredito "bug de plataforma incondicional" acima está REFUTADO; o CS NÃO será aposentado.** Comparação com o V5 (mesmo `CustomRatesUpdate`, atravessava meia-noites sem morrer) mostrou que a morte era **auto-infligida**: (a) CS criado **sem sessões 24/7** (causa-raiz — MT5 recusa o update na virada de dia; o V5 setava 24/7 nos 7 dias); (b) spec-setters reaplicados a cada `OnInit` apagando o histórico no re-attach; (c) sink sem recovery. "Independente de barra-no-futuro" e "`CustomRatesReplace`/`CustomTicksAdd` compartilham o container bugado" eram conjectura sem fonte (a corrupção de fórum é CONDICIONAL a múltiplos CS, sem repro com CS único). **Decisão: manter+corrigir o CS**; ADR-031 passa de "aposentar" a "manter+corrigir" (ainda não escrita); ADR-020/021/023/023-A **não** são superseded (cap = higiene). 4 fixes aplicados (sessões 24/7; specs só na criação; recovery no sink; marcador em `sink.lastBarTime`), compilam 0/0. **0 `CS UPDATE FAIL` em 06-02** nos restarts confirma o fix do re-attach; sobrevivência à meia-noite (06-02→06-03) **pendente de confirmação empírica**. Detalhe na nota REVERSÃO 2026-06-02 da ADR-023-A em `ARCHITECTURE.md`.

### Added
- **ADR-030 — StressLab credível, parte 2: adversidade simétrica entrada/saída + requote independente + teste de integração (2026-05-29).** Fecha os 3 furos do StressLab achados na auditoria de 2026-05-29 (pré-requisito do stress runner / Fase 9 slice 2).
  - `MQL5/Include/MKS-ULTIMATE/StressLab/CMksStressLabBroker.mqh` — reescrito. (a) **Saídas estressadas**: `Close` aplica slip adverso e o wrapper ganha `OnTick`/`PollAutoCloses` que delegam ao broker simulado e aplicam slip a cada auto-close de SL/TP (furo #1 — antes só a entrada era estressada; o stop hit escapava). (b) **Requote independente do underlying** (furo #2): modelado como sorteio interno — cada requote = re-cotação adversa, `maxRequotes` consecutivos = `REJECTED` (10004); antes o loop só rodava se o underlying não preenchesse, e o broker simulado sempre preenche → knob morto. (c) Coupling opcional via 4º param `exitSim` (`CMksSimulatedBroker*`, default NULL) — destrava as saídas sem quebrar o uso genérico com mocks. `CMksStressMetrics` ganha `closesStressed`/`autoClosesStressed`. **`CMksSimulatedBroker` intocado.**
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksStressLabReport.mq5` — `Test_RPT_CaptureFromRealPipeline`: roda um pipeline **real** (sim + stress, ticks, Send/Close/auto-close) e captura as métricas reais no report (furo #3 — antes as métricas eram hand-built via `MakeMetrics`, então um bug de contabilidade passaria batido).
  - `docs/ARCHITECTURE.md` — **ADR-030** (6 cláusulas, 4 alternativas rejeitadas, fronteiras).
  - **Validação MT5 (2026-05-30):** `Test_CMksStressLabBroker` **112/112 assertions, 19 tests, 0 failed** ✅; `Test_CMksStressLabReport` **32/32 assertions, 5 tests, 0 failed** ✅. Os 3 furos (saída estressada, requote interno, report sobre pipeline real) validados empiricamente.
- **ADR-029 — framework v1 é hedging-only: detecta e recusa conta netting/exchange (2026-05-29).**
  - `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — código `MKS_ERR_BROKER_NETTING_UNSUPPORTED = 204` (faixa Broker).
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksMt5Broker.mq5` — **novo**: valida `Init` aceitando hedging e recusando netting/exchange com 204, e precedência do guard de nulos (mocks, sem API real do MT5). Os 3 arquivos `.mq5` afetados compilam **0 erros / 0 warnings** via MetaEditor64 headless (ColorReversal, Test_CMksMt5Broker, Test_MksMt5BrokerLive). **Teste RODADO no MT5 (2026-05-29): `11/11 assertions, 4 tests, 0 failed` ✅** — hedging aceito, netting/exchange recusados com 204, precedência do guard de nulos confirmada. **⚠️ Resta (oportunístico, não-bloqueante):** exercitar o guard contra uma **conta netting real** — o unit test usa mocks; o popup `Alert` no live só dispara numa conta netting de fato.
  - `docs/ARCHITECTURE.md` — **ADR-029** (6 cláusulas, 5 alternativas, fronteiras; relações com ADR-013/017); §2 (árvore) sincronizada com o disco (`Strategy/`, `ITradeVisualizer`, `CMksChartPainter`, `CMksAuditLogSink`, `ColorReversal`).
  - `docs/Projeto.md` §9 — "hedging-only (v1)" registrado como decisão-chave.

### Changed
- `MQL5/Include/MKS-ULTIMATE/Core/Broker/CMksMt5Broker.mqh` — `Init()` recusa qualquer `MarginMode != ACCOUNT_MARGIN_MODE_RETAIL_HEDGING` (whitelist hedging — netting e exchange caem) com `MKS_ERR_BROKER_NETTING_UNSUPPORTED`, antes de qualquer ordem. Camada única de detecção; todo EA de trade herda. O ramo netting de `Close`/`Modify` (ADR-017 §5) vira código dormente (mantido para fork futuro). ADR-029.
- `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — `OnInit` faz fail-fast (antes de criar Custom Symbol/arquivos) com `Alert()` (popup) + log ERROR + `INIT_PARAMETERS_INCORRECT` se a conta não for hedging. Mesmo molde do guard de Custom Symbol existente. Vale em tester e live. ADR-029.
- `docs/ROADMAP.md` — **Fase 9 status corrigido** de "Não iniciada" para "Em andamento" (MVP validado em 3 ambientes: testes 46/46, tester 617 trades, demo live 11 ordens reais; falta slice 2 stress runner); limitação netting da Fase 5 marcada **resolvida por ADR-029**; nota "648/41 assertions" reenquadrada como snapshot histórico de 2026-05-22 (total corrente maior). Sync da auditoria 2026-05-29.

### Added
- **Fase 9 — fill histórico no live (feedback empírico 2026-05-27).**
  - `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — input `InpHistoricalFillDays = 3`: no live, popula CS + `.mksbk` com 3 dias de bricks históricos ANTES de operar, via `RunHistoricalFill` (CopyTicksRange + warm-up). Resolve "CS nasce sem histórico".
  - `MQL5/Include/MKS-ULTIMATE/Strategy/CMksColorReversalStrategy.mqh` — `SetWarmup(bool)`: durante o fill histórico a estratégia **rastreia a direção dos bricks mas NÃO opera** (sem Send/Close sobre bricks do passado). O primeiro flip live decide corretamente (direção do último brick histórico registrada). Segurança: sem warm-up, o fill abriria "trades" no passado.
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksColorReversalStrategy.mq5` — teste novo `Test_CR_WarmupTracksDirectionNoTrades` (flips em warm-up → 0 trades; pós-warm-up → 1o flip opera na direção certa).

### Changed
- **Visualização (ADR-028) — removido o desenho de renko no tester (gambiarra); painter vira só marcadores de trade.** Após validação visual no tester, decidida a separação limpa (ADR-028 nota 2026-05-28). O tester do MT5 proíbe criar Custom Symbol (err 4014), então qualquer renko desenhado lá é maquiagem sobre M1 — fingir bricks como retângulos + esconder candles brigava com a plataforma e contrariava ADR-015 (tester é ferramenta) + ADR-020 (CS é a viz). Removidos de `CMksChartPainter`: `DrawBrick`, `ENUM_MKS_RENKO_VIEW` (OVERLAY/CLEAN), `HideCandlesOnce`, `CMksBrickPainterSink`; do `ColorReversal.mq5`: input `InpRenkoView` + wiring do brick painter sink. O painter agora desenha **apenas marcadores de trade** (setas ▲▼✗ + linha P&L) — no live sobre o CS renko, no tester sobre os candles M1. Renko visual é do CS (live) ou do visualizador de backtest futuro (slice A2: `.mksbk` → CS).

### Added
- **Visualização de trades (ADR-028) — camada de chart objects.**
  - `docs/ARCHITECTURE.md` §3 — **ADR-028** (camada de visualização de trades: chart objects, canal de eventos, paridade preservada). Materializa a alternativa (f) da ADR-020 ("indicador renko canvas / OBJECT_RECTANGLE", adiada). Marcadores de entrada/saída + linha P&L desenhados como chart objects — único mecanismo que funciona idêntico em tester (CS proibido lá) e live. Documenta a garantia de paridade testável (viz ON vs OFF → `.mksbk` byte-a-byte idêntico) e 5 alternativas rejeitadas.
  - `docs/ARCHITECTURE.md` §3 — **ADR-023 promovida de Proposta para Aceita** (critério de produto acionado pela ADR-028: "agradável para usuário final", marcadores ancorados em tempo). Timeline híbrida real+bump agora vale.
  - `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/ITradeVisualizer.mqh` — interface nova. `MarkEntry(timeMsc, side, price, positionId)` + `MarkExit(timeMsc, price, positionId)`. Puro output; injeção opcional; NULL = sem viz, zero mudança de comportamento.
  - `MQL5/Include/MKS-ULTIMATE/Core/Output/CMksChartPainter.mqh` — implementa `ITradeVisualizer`. Setas de entrada (233 up/BUY, 234 down/SELL) + saída (251 x, cor por P&L) + linha conectora `OBJ_TREND` colorida por lucro/prejuízo. Opcionalmente retângulos de brick (`OBJ_RECTANGLE`) no tester. Prefixo `MKSCR_VIZ_` + `Clear()`. No-op em backtest não-visual. Inclui `CMksBrickPainterSink` (adaptador IRenkoSink→painter, já que MQL5 não tem herança múltipla).
  - `MQL5/Include/MKS-ULTIMATE/Core/Testing/Mocks/CMksRecordingVisualizer.mqh` — mock que conta MarkEntry/MarkExit para testes.
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksColorReversalStrategy.mq5` — 2 testes novos: `Test_CR_VisualizerReceivesEntryAndExit` (eventos disparam, ancorados no closeTimeMsc do brick) e `Test_CR_VisualizerParityOnVsOff` (mesma sequência com viz NULL vs mock → Sends/Closes/estado idênticos — paridade ADR-028 §7).

### Changed
- `MQL5/Include/MKS-ULTIMATE/Core/Output/CMksCustomSymbolSink.mqh` — **timeline híbrida real+bump (ADR-023)**. `OnBrickClose` usa `brickTime = max(closeTimeMsc/1000, nextBarTime)` em vez de `+60s` cego. Eixo X do CS vira aproximadamente-real (granularidade de segundos em mercado calmo, bump em frenético). Novo campo `lastBarTime` (âncora p/ marcadores). Bricks ficam distribuídos no tempo real.
- `MQL5/Include/MKS-ULTIMATE/Strategy/CMksColorReversalStrategy.mqh` — ganha 8º parâmetro opcional `ITradeVisualizer *visualizer = NULL`. Chama `MarkEntry` após Send filled, `MarkExit` após Close filled ou auto-close externo detectado. Sem visualizer = comportamento idêntico (paridade).
- `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — input novo `InpShowTradeMarkers` (default true). Composition root localiza o chart do CS no live (`FindChartIdBySymbol`), cria `CMksChartPainter` (drawBricks=tester), injeta na strategy. `nextBarTime` inicia em 0 (ADR-023 §1). `painter.Clear()` no OnInit (start limpo). `g_nextBarTime=0`.

### Added
- `MQL5/Include/MKS-ULTIMATE/Core/Output/CMksAuditLogSink.mqh` — método `Flush()` (FileFlush sem fechar handle). Permite inspeção do audit TSV mid-sessão (`wc -l`, `tail`) numa demo live de horas sem destacar o EA.
- `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — **checkpoint de observabilidade a cada 60s** no OnTick (wall-clock via `GetTickCount`, fora do tester). Patcheia header do `.mksbk` via `g_writer.Checkpoint()` + `g_auditSink.Flush()`. Motivação concreta (2026-05-27): na primeira demo live bem-sucedida, os números só ficaram visíveis quando a sessão fechou (deinitReason 5) — sem flush periódico, monitorar uma demo de horas era cegueira. Agora `.mksbk` e audit TSV crescem observáveis em tempo real. Mesmo padrão do Producer.

### Changed
- `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — **guard de Custom Symbol no OnInit** (descoberto em 2026-05-27: demo live rodou a noite inteira sem abrir ordem). Causa-raiz: o EA estava anexado ao gráfico de um Custom Symbol de visualização (`XAUUSDm.MKS_3`/`.MKS_1`, criado pelo Producer), que é container ESTÁTICO de bricks históricos e não recebe feed de ticks ao vivo → `CopyTicks` retornava 0 para sempre → `ticks:0` em todas as sessões → zero bricks, zero flips, zero ordens. Fix: fora do tester, OnInit recusa com `INIT_PARAMETERS_INCORRECT` + log ERROR + Print explícito se `SymbolInfoInteger(symbol, SYMBOL_CUSTOM)` é true. Mensagem instrui a anexar no símbolo REAL do broker (XAUUSDm sem sufixo `.MKS_*`). Erro de categoria fácil de cometer (o gráfico do CS *parece* os bricks da estratégia), agora blindado estruturalmente.
- `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — **3 fixes para rodar em Strategy Tester** descobertos durante validação empírica de 2026-05-27:
  - `g_isTesting = MQLInfoInteger(MQL_TESTER)` detectado em OnInit; quando true, **pula `CustomSymbolCreate` e CSSink** (MT5 retorna erro 4014 = `ERR_FUNCTION_NOT_ALLOWED` em tester — limitação documentada). CS é puramente visual, strategy recebe bricks via IRenkoSink direto, sem perda funcional.
  - `EnsureCustomSymbolReady` reescrita conforme versão canônica do Producer: pré-check `SYMBOL_CUSTOM`, código de erro real `5304` (era buggado `4302`), setters completos de propriedades (`SYMBOL_DIGITS`, `SYMBOL_POINT`, `SYMBOL_TRADE_TICK_SIZE`, etc.).
  - `g_mt5Broker.Init(err)` agora é chamado explicitamente após construtor (estava faltando — broker retornava `MKS_ERR_BROKER_NOT_INITIALIZED` em todas as Sends).
  - `OnTradeTransaction(...)` adicionado ao EA roteando para `g_mt5Broker.OnTradeTransactionEvent` (fallback necessário para sincronização com MT5 trade events).
- **Validação empírica Fase 9 em Strategy Tester (2026-05-27):** XAUUSDm, 2026-05-20 → 2026-05-26, 100% qualidade histórica, 1.112.064 ticks reais → 1.275 bricks → 617 flips → **617/617 Sends preenchidos (0 rejeições)** → 319 closes via strategy + 297 auto-trigger SL via `CMksMt5Broker` + `IPositionBook.IsOpen` → Net -16.26 USD (esperado: estratégia "reversão de cor pura sem filtros" não tem edge por design — ROADMAP §Fase 9 literal). Max DD 1.14%, Profit Factor 0.98, Sharpe -1.21 — confirma empiricamente que o core compõe, executa, gasta custos no equity (eixo 3 do V5-POSTMORTEM mitigado) e o Risk Manager protege a conta.

### Added
- **Fase 9 MVP — primeiro EA end-to-end usando o core completo.**
  - `MQL5/Include/MKS-ULTIMATE/Strategy/CMksColorReversalStrategy.mqh` — estratégia minimalista que implementa `IRenkoSink`. Lógica: em cada flip de cor (BULL→BEAR ou inverso) fecha posição corrente (se houver) e abre nova posição na direção da nova cor. SL fixo em pontos (input), sem TP — saída só por flip seguinte ou SL hit (auto-trigger do broker). Auto-detach: consulta `IPositionBook.IsOpen(positionId)` no início de cada `OnBrickClose` antes de decidir; se broker fechou externamente (SL hit em live, auto-close do `CMksSimulatedBroker` em backtest), zera state interno sem chamar `Close` fantasma. Métricas agregadas (bricksSeen, flipsDetected, sendsAttempted/Filled/Rejected, closesAttempted/Filled/Rejected, autoDetected) via getter `Metrics()`. Determinística: estado interno sem RNG, mesma sequência de bricks + mesmo broker → mesma sequência de Send/Close.
  - `MQL5/Experts/MKS-ULTIMATE/ColorReversal.mq5` — EA composition root da Fase 9. Monta: `CMksMt5Symbol` + `CMksMt5Account` + `CMksMt5Clock` + `CMksLogger` (file + journal) + `CMksFixedBrickSizer` + `CMksBrickFileWriter` + `CMksBrickWriterSink` + `CMksCustomSymbolSink` (visualização) + `CMksAuditLogSink` (TSV humano-legível) + `CMksMultiSink` (dispatcher) + `CMksRenkoBuilder` (classic geometry, soft K-recovery default) + `CMksMt5PositionBook` (escopo símbolo+magic) + `CMksAccountSnapshot` + `CMksRiskManager` (3 camadas: trade+strategy+account) + `CMksMt5Broker` (live) + `CMksRiskGatedBroker` (gateado pelo risk manager) + `CMksFixedLotSizer` ou `CMksPercentRiskSizer` (input `InpLotMode`) + `CMksColorReversalStrategy` (adicionada ao multiSink). OnTick usa `CopyTicks(COPY_TICKS_ALL)` para alinhamento de feed (consistente com Producer pós-refactor 2026-05-26). Magic default 527001. Session summary no OnDeinit inclui métricas da strategy + caminhos dos arquivos gerados.
  - `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksColorReversalStrategy.mq5` — 11 testes novos cobrindo: primeiro brick sem ação, brick repetido sem ação, flip BULL→BEAR sem posição prévia abre SELL, flip BEAR→BULL abre BUY, sequência de múltiplos flips, fields da request, auto-detach quando book diz "fechado", auto-detach + flip subsequente abre limpo, sem book preserva legacy, Send rejeitada não corrompe state, ResetMetrics.

### Added
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/IPositionBook.mqh` — método novo `IsOpen(positionId) → bool`. Permite a callers (`CMksTradeManager` em particular) consultar se uma posição específica continua aberta no escopo do book. Essencial para detectar fechamento externo (SL/TP hit, manual close, auto-close do `CMksSimulatedBroker` pós-ADR-027 §7.3).
- `MQL5/Include/MKS-ULTIMATE/Core/Position/CMksMt5PositionBook.mqh` — implementação de `IsOpen` via `PositionGetTicket` iterando `PositionsTotal`, com filtro de escopo símbolo+magic. O(N) por chamada (N tipicamente << 10 — aceitável).
- `MQL5/Include/MKS-ULTIMATE/Core/Testing/Mocks/CMksFakePositionBook.mqh` — array `m_closedIds[]` + método `MarkClosed(positionId)` para testes. `IsOpen` retorna false se id estiver na lista; default é "aberto" para preservar simplicidade dos testes pré-existentes.
- `MQL5/Include/MKS-ULTIMATE/Core/Trade/CMksTradeManager.mqh` — **auto-detach** quando `IPositionBook` (opcional, 5º arg do construtor) indica posição fechada. `Update()` consulta `book.IsOpen(positionId)` antes de qualquer ação; se false, zera `m_attached`, marca `step.autoDetached=true` e retorna sem chamar broker. Sem book = comportamento legado preservado (backward-compat total). Flag nova `autoDetached` no `MksTradeManagerStep`. Resolve a limitação da Fase 5 documentada na auditoria 2026-05-25 e fecha o gap aberto pela ADR-027 §7.3.
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksTradeManager.mq5` — 3 testes novos: `Test_TM_AutoDetach_WhenPositionClosedExternally`, `Test_TM_AutoDetach_SubsequentUpdateIsNoOp`, `Test_TM_NoBook_LegacyBehaviorPreserved`.
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksRiskManager.mq5` — 2 testes novos: `Test_Risk_CheckOrder_AutoUpdatesSnapshotForFreshness` (EA "esquece" `snapshot.Update()`, equity baixa direto no `IAccount`, CheckOrder rejeita por daily loss usando equity fresca → prova que CheckOrder atualiza o snapshot internamente), `Test_Risk_CheckOrder_AutoUpdateNoOpWithoutSnapshot`.

### Changed
- `MQL5/Include/MKS-ULTIMATE/Core/Risk/CMksRiskManager.mqh` — `CheckOrder` chama `m_snapshot.Update()` no início se `m_snapshot != NULL`. Garante que `DayPnLPct`/`DrawdownPct`/`Equity` sejam computados sobre balance e equity correntes, mesmo se o EA esquecer de chamar `snapshot.Update()` por tick. Idempotente — `snapshot.Update` já trata rollover de dia + peak monotônico. Resolve limitação da Fase 6 documentada na auditoria 2026-05-25.
- `docs/ROADMAP.md` — Fase 4.5 status atualizado de "validação empírica end-to-end pendente" para "validação empírica concluída (2026-05-26): verify-parity.ps1 exit 0 + diff dos audit TSV 0 diferenças". Limitações resolvidas nas Fases 5 e 6 marcadas com strikethrough + nota da resolução. Fase 9 fica plenamente desbloqueada.

### Added
- `docs/ARCHITECTURE.md` §3 — **ADR-027** (StressLab credível: latência aplicada, spread composto, SL/TP auto-disparados). Materializa as três correções listadas como pré-requisito da Fase 9 na auditoria 2026-05-25. Três sub-cláusulas: (§7.1) novo parâmetro `latencyDriftPointsPerMs` em `CMksStressParams` — latência sorteada via Gaussiana e convertida em slip adverso (modela "preço se moveu durante viagem da ordem"); (§7.2) novo parâmetro `baselineSpreadPoints` — `spreadMultiplier` compõe sobre baseline real do CostModel (`extraHalfSpread = (mult-1) × baseline/2` por lado, equivalente a "spread efetivo mult× maior"), em vez da fórmula buggada anterior que ignorava o spread real; (§7.3) `CMksSimulatedBroker.OnTick` chama novo `CheckSlTpTriggers` que detecta SL/TP via bid/ask proxy do half-spread do CostModel e fecha posições pelo CostModel (close inclui slippage realista de SL hit). Fila `MksSimAutoCloseEvent` drenável via `PollAutoCloses`. Métricas novas no `CMksStressMetrics`: `latencyTotalMs`, `latencyMaxMs`. ADR documenta 5 alternativas rejeitadas e 6 fronteiras explícitas. Fase 7 do ROADMAP atualizada — bloco "Limitações conhecidas — pré-requisito Fase 9" removido inteiro.
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksStressLabBroker.mq5` — 4 testes novos: `Test_SL_SpreadMultiplierComposesWithBaseline` (composição correta com baseline), `Test_SL_SpreadMultiplierNoOpWithoutBaseline` (preserva backward-compat quando baseline=0), `Test_SL_LatencyDriftAdverseFixed` (100ms × 0.01pts/ms = +1pt adverso), `Test_SL_LatencyZeroDriftPreservesLegacy` (drift=0 mantém semântica "informativa" antiga). Teste antigo `Test_SL_SpreadMultiplierAddsSlip` removido (materializava a fórmula buggada).
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksSimulatedBroker.mq5` — 5 testes novos: `Test_AutoTriggerSlBuy`, `Test_AutoTriggerTpSell`, `Test_AutoTriggerStaysOpenWhenNoHit`, `Test_AutoTriggerHalfSpreadBidProxy` (gatilho usa bidProxy/askProxy via half-spread do CostModel, não bid/ask cru do tick), `Test_AutoTriggerDeterminism`.

### Changed
- `MQL5/Include/MKS-ULTIMATE/StressLab/CMksStressParams.mqh` — `CMksStressParams` ganha `baselineSpreadPoints` (default 0) e `latencyDriftPointsPerMs` (default 0). Presets mantêm defaults zerados — caller seta conforme símbolo (acoplar valor ao preset acopla preset a símbolo, anti-padrão). Comentários do header explicam composição e heurística (XAU/Exness ≈ 0.01pts/ms).
- `MQL5/Include/MKS-ULTIMATE/StressLab/CMksStressLabBroker.mqh` — `ApplySlippageToFill` reformulada. Slip total = `sampledSlippage + extraHalfSpread + slipFromLatency`. Métricas `latencyTotalMs`/`latencyMaxMs` agregadas. Novo helper `SampleLatencyMs()` (clampa em zero, no-op quando mean=stdev=0 — preserva determinismo de testes que não tocam latency).
- `MQL5/Include/MKS-ULTIMATE/Core/Broker/CMksSimulatedBroker.mqh` — `OnTick` chama `CheckSlTpTriggers` após atualizar mid. Nova struct `MksSimAutoCloseEvent`, fila `m_autoCloseEvents[]`, contador monotônico `m_autoCloseTotal`. Métodos novos: `CheckSlTpTriggers`, `PollAutoCloses(out[])`, `PendingAutoCloses()`, `AutoCloseTotal()`.
- `docs/ROADMAP.md` — Fase 7 perde bloco "Limitações conhecidas — pré-requisito para a Fase 9". Status muda de "Concluída com limitações" para "Concluída". Fase 9 fica desbloqueada.

### Added
- `MQL5/Include/MKS-ULTIMATE/Core/RenkoBuilder/CMksRenkoBuilder.mqh` — **soft recovery de gap estrutural** (ADR-011 nota 2026-05-26). Novo parâmetro do construtor `kRecoverAfter = 5` (default; 0 desabilita): após N rejeições K consecutivas com mids agrupados (variância ≤ `S`), o builder reanchora `m_lastClose = mid` e marca `m_hasFirstBrick = false` (próximo movimento define direção). Quebra o cenário de travamento permanente do builder quando `m_lastClose` fica stale após gap maior que `K·(1-PO)·S`. Reset do contador em qualquer tick aceito — exige sequência consecutiva. Helper `HandleKExceeded` consolida as duas paths (primeiro brick / subsequentes) que antes duplicavam o mesmo erro 102.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — novo código `MKS_ERR_RENKO_RECOVERED_FROM_GAP = 105` (faixa RenkoBuilder). Sinaliza que o builder reanchorou após gap estrutural detectado pelo soft recovery. Distinto do 102 (que continua sendo a rejeição "dura" sem reanchor).
- `MQL5/Include/MKS-ULTIMATE/Core/Data/CMksBrickFileWriter.mqh` — métodos `Checkpoint(err)` e `Flush()` simétricos ao `CMksTickFileWriter`. Permitem patch periódico de `brickCount`/`timeMscFirst`/`timeMscLast` no header do `.mksbk` durante live sem fechar o handle. Crash do terminal deixa o arquivo parcialmente válido — leitor enxerga até o último checkpoint. `createdAtMsc` continua sendo gravado só no `Close` (padrão idêntico ao TickFileWriter).
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — checkpoint periódico do `.mksbk` a cada 60s no live mode (contador no OnTimer 1Hz). Flush simultâneo do `.mkstick` paralelo (modo paridade). Trata código 105 distinto do 102 (contador `g_k105Recovered`, audit via `RecordKExceeded(tickSeq, mid, -105)`, log warning explícito). Session summary ganha campo `err105Recovered`.
- `MQL5/Experts/MKS-ULTIMATE/Replayer.mq5` — trata código 105 simétrico ao Producer (contador `g_ticksK105`, audit). Replay summary ganha campo `ticksK105Recovered`.

### Changed
- `MQL5/Experts/MKS-ULTIMATE/Replayer.mq5` — input `InpBrickSizePts` renomeado para `InpBrickSize` (consistência com Producer após refactor de 2026-05-25). Sem mudança de comportamento — só naming.
- `docs/ARCHITECTURE.md` §3 (nota da ADR-011) — atualizada de "follow-up pendente" para "soft recovery implementado", com parâmetros concretos (`kRecoverAfter=5`, tolerância de variância = `S`) e referência ao novo código 105.

### Added
- `MQL5/Include/MKS-ULTIMATE/Core/Output/CMksAuditLogSink.mqh` — sink TSV (tab-separated) que escreve uma linha por brick fechado com todos os campos (`idx, seq, dir, open, close, high, low, trigger, tickId, closeTimeMsc, M`) em precisão `%.6f`. Comentários `# K_EXCEEDED`, `# INVALID_TICK`, `# STREAM_HALTED` registram eventos anômalos do builder durante a captura. Permite **diff humano-legível** entre audit do Producer e audit do Replayer — complementa o `fc /b` binário do `verify-parity.ps1` revelando primeira linha divergente em texto.
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — input novo `InpParityRunMode` (default false). Quando `true`, o Producer grava **`.mkstick` paralelo** no mesmo loop do `OnTick` (anchor sincronizado por construção, sem precisar de TickRecorder Service separado) + **`audit.tsv`** para diff humano-legível. Resultado: pipeline de teste de paridade canônica ADR-024 atinge **1 EA anexado** + 1 Replayer ao final. Anchor inicial entre `.mksbk` e `.mkstick` é idêntico por construção (mesmo loop, mesma origem). Eventos 102/103/104 registrados no audit via `RecordKExceeded/RecordInvalidTick/RecordStreamHalted`. Session summary do logger ganha campos `parityMode`, `parityMksTickPath`, `parityTickCount`, `parityAuditPath`. Caminhos: `.mkstick` paralelo em `MKS-ULTIMATE\Ticks\<symbol>_<TS>_parity.mkstick` (sufixo `_parity` distingue de captura contínua do TickRecorder Service); audit em `MKS-ULTIMATE\Logs\Producer_audit_<symbol>_<TS>.tsv`.
- `MQL5/Experts/MKS-ULTIMATE/Replayer.mq5` — input novo `InpAlsoWriteAudit` (default false). Quando `true`, gera `audit.tsv` simétrico ao do Producer (mesmos campos, mesma precisão). Diff posterior `fc /l` ou `diff` entre os dois `audit.tsv` aponta primeira linha divergente em texto humano. Builder agora alimenta `g_multiSink` (em vez de `g_sink` direto) quando audit ligado — multiSink despacha para writer (.mksbk) + auditSink (audit.tsv). Eventos 102/103/104 registrados no audit do mesmo modo do Producer. Replay summary ganha campo `auditPath`.

### Changed
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — **refactor do `OnTick` de `SymbolInfoTick` para `CopyTicks(COPY_TICKS_ALL, lastSeenMsc, 0)`** — alinha o feed do Producer ao do TickRecorder (mesma API MQL5, mesma janela), tornando a paridade canônica ADR-024 §regra 7c (`fc /b live.mksbk replay.mksbk`) **atingível**. Achado empírico de 2026-05-26: na arquitetura anterior, Producer via `OnTick → SymbolInfoTick` enxergava apenas o "tick mais recente" quando o callback disparava; em bursts, ticks intermediários eram perdidos. Mesma sessão de 12h com TickRecorder em paralelo: Producer viu 16.615 ticks → 248 bricks; replay sobre o `.mkstick` capturado pelo TickRecorder (que usa `CopyTicks`) consumiu 34.083 ticks → 364 bricks. Os trigger prices dos primeiros bricks eram **idênticos byte-a-byte** entre live e replay — confirmando que o builder é determinístico — mas open/close/low divergiam por offset constante (~0.037 USD) por causa do anchor inicial diferente entre as duas APIs. Após refactor, Producer e TickRecorder consomem mesma fonte de ticks; mesmos triggers + mesmo anchor inicial = bricks idênticos. Novo `g_lastSeenMsc` (variável global) mantém janela incremental; anchor inicial via `SymbolInfoTick` em `FinishInitAndGoLive` (mesmo padrão `TickRecorder.OnStart`) evita que primeiro `CopyTicks(fromMsc=0)` retorne cache histórico inteiro. Dedup por `timeMsc <= lastSeenMsc` + filtro mínimo `bid<=0 && ask<=0` (lixo estrutural de slot vazio do CopyTicks). Log de "ready, processing live ticks" passa a incluir `"anchorMsc"`.

### Added
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-011 (premissa do limiar K e cenário de `lastClose` stale). A §regra 4 afirmava que "M > K é quase certamente tick corrompido". Validação empírica de 2026-05-26 mostrou cenário legítimo não previsto: fill histórico com S pequeno + tempo offline → primeiro tick live cruza M > K → builder rejeita SEM atualizar `m_lastClose` → todos os próximos ticks legítimos rejeitados pelo mesmo motivo → **builder trava permanentemente**. A premissa estava incompleta — corrupção isolada (1 tick outlier) é caso correto da §4, mas gap estrutural (N ticks legítimos com mids próximos entre si mas distantes do `m_lastClose` stale) não. A nota distingue os dois casos, fixa mitigação operacional verificada (`InpHistoricalFillDays=0` elimina estruturalmente OU `K` dimensionado para cobrir o maior gap esperado, heurística `K ≥ 100/S` para XAU com fill histórico), e registra como follow-up arquitetural um "recovery inteligente" que reanchora `m_lastClose = mid` após N rejeições consecutivas com variância baixa entre mids (distingue corrupção de gap). ADR-011 não é alterada.

### Changed
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — comentário do input `InpThresholdLimit` ganha heurística operacional: "dimensionar p/ cobrir maior gap entre fim-do-fill e tick-live; com fillDays=0, K=20 OK; com fillDays>0 e S pequeno, considerar K ≥ 100/S". Materializa a mitigação documentada na nota da ADR-011 acima.

### Changed
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — refactor de configuração do sizer de brick. (a) Input `InpBrickSizePts` renomeado para `InpBrickSize`, com comentário corrigido explicitando que a unidade efetiva é **price units** (USD para XAU, EUR para EURUSD, etc — não é ponto do símbolo apesar de "Pts" interno). Auditoria de 2026-05-25 confirmou semântica via leitura de código: `CMksRenkoBuilder` soma `SizePoints()` direto no preço sem multiplicar por `Symbol::Point()`. (b) Novo `enum ENUM_BRICK_SIZER_MODE { SIZER_MODE_FIXED, SIZER_MODE_ATR }` + input `InpSizerMode` permitem o operador escolher entre `CMksFixedBrickSizer` (default) e `CMksAtrBrickSizer` (ADR-018, pronto desde 2026-05-21 mas até hoje inacessível ao Producer). (c) Novos inputs `InpAtrPeriod=14`, `InpAtrMultiplier=1.0`, `InpAtrDefaultSize=1.5`, `InpAtrMinSize`, `InpAtrMaxSize` ativos apenas quando `InpSizerMode=SIZER_MODE_ATR`. (d) Factory `CreateSizer(mode)` instancia o sizer correto; `g_sizer` agora é `IBrickSizer*` (polimórfico) em vez de `CMksFixedBrickSizer*`. (e) Naming do CS suporta ambos os modos: Fixed = `<symbol>.MKS_3` (igual antes), ATR = `<symbol>.MKS_ATR14x100` (period × round(multiplier×100)). Helpers `FormatFixedSizeLabel`/`FormatAtrSizeLabel`/`CurrentSizerLabel`/`SizerModeName` isolam a formatação. (f) Header do `.mksbk` e sink do CS gravam `sizer.SizePoints()` inicial — em Fixed = `InpBrickSize`; em ATR = `InpAtrDefaultSize` durante warm-up, depois ATR×mult corrente. (g) Painel SaaS Navy lê tamanho corrente do sizer (em ATR varia, mas só uma leitura no `LiveMode`). Log de starting passa a incluir `sizerMode`, `atrPeriod`, `atrMult`, `atrDefault`. Auditoria identificou estes 3 problemas relacionados (nome enganoso + ATR ocioso + lacuna de UX) — todos endereçados num único refactor.
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/IBrickSizer.mqh` — comentário do método `SizePoints()` clarificado: explicita que a unidade é **price units do símbolo** (USD para XAU, etc — não Symbol::Point()), e que o nome `SizePoints` é histórico mas a semântica é "valor somado direto no preço pelo builder". Não altera contrato — só corrige documentação enganosa identificada na auditoria.
- `MQL5/Include/MKS-ULTIMATE/Core/RenkoBuilder/CMksFixedBrickSizer.mqh` — `@responsibility` no header do arquivo + comentário acima da classe clarificam que `sizePoints` recebido no construtor é **price units**, não pontos do símbolo. Argumento `sizePoints` mantido por compatibilidade com a interface `IBrickSizer`.

### Added
- `docs/CHECKPOINT-2026-05-25-audit.md` — adendo da sessão de tarde/noite de 2026-05-25. Documenta auditoria completa entregue (10 pilares, 67 `.mqh` + 38 `.mq5` cobertos), execução do **Lote A** (sync documental: ROADMAP/ARCHITECTURE/CHECKPOINTS), execução do **Lote B2** (nota ADR-020 sobre indicadores) e **Lote B3** (nota ADR-013 §5), e **suspensão do B1** após o dono apontar padrão de 3 erros analíticos consecutivos (B2, B3, B1) — todos com o mesmo defeito: salto da estrutura observada para conclusão arquitetural sem ponte empírica. Próximo passo fixado: validação empírica E2E do pipeline ADR-024 antes de qualquer ADR-025 sobre concorrência Producer/TickRecorder. Lição operacional registrada: "observação → hipótese → dado → ADR, nessa ordem".
- `docs/CHECKPOINTS.md` — entrada do `CHECKPOINT-2026-05-25-audit.md` adicionada à tabela cronológica.
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-013 (**status da dívida da §5 — detecção de broker**). A ADR-013 §5 registrou como dívida "nova ADR para detecção de broker pós-Slice 3, com evidência empírica do que varia entre brokers". Slice 3 e subsequentes (4, 4.5, 5a, 5b, 6.1–6.3, 7, 8) foram fechados sem acionar a dívida. A nota registra que ela **permanece em aberto sem ação requerida** — variação entre brokers já absorvida por `ISymbol`/`IAccount` (ADR-016) e variações de execução por `CMksMt5Broker` (ADR-017); evidência empírica permanece em 1 broker (Exness), e §4 deste documento proíbe arquitetura no vazio. Fixa 3 gatilhos explícitos de reabertura. ADR-013 não é alterada.
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-020 (**alcance da regra 1** — indicadores como visualização). A regra 1 da ADR-020 listava "indicador customizado" entre os agentes proibidos de ler o CS, mas indicadores `.mq5` em `MQL5/Indicators/MKS-ULTIMATE/` precisam ler o CS para fazer o trabalho de visualização. A nota restringe o alcance da §1 ao caminho `Strategy → iCustom(indicator) → CS` (que já é fechado pela REGRAS §1.9 via proibição de `iCustom` em código de estratégia) e deixa indicadores como camada de visualização explicitamente **fora** do escopo. Resolve conflito textual identificado na auditoria 2026-05-25. ADR-020 não é alterada.
- `docs/INDICATORS.md` §1.1 — seção nova "Indicadores são camada de visualização — paridade pelo lado da estratégia". Documenta que indicadores leem o CS via API global e referencia cruzadamente a nota da ADR-020 + REGRAS §1.9. Estabelece que estratégia futura que precise de RSI/MACD/etc. calcula sobre `MksBrick` direto, não via `iCustom`.
- `docs/CHECKPOINTS.md` — índice cronológico dos 11 checkpoints de sessão acumulados (2026-05-20 → 2026-05-25). Cada linha aponta um marco (slice fechado, ADR aceita, fase concluída). Resolve o problema de "achar o checkpoint relevante" sem mover arquivos (mantém links cruzados intactos). Documenta explicitamente que checkpoints **não estão na ordem de leitura** do `CLAUDE.md` — só são consultados sob demanda histórica.
- `CLAUDE.md` — lista de Documentos de referência ganha entrada para `docs/CHECKPOINTS.md` com nota "**não está na ordem de leitura**".

### Changed
- `docs/ROADMAP.md` — Fases 5 (Trade Management), 6 (Risk Management) e 8 (Logging) reclassificadas de "Não iniciada" / "Parcialmente concluída" para **Concluída**, com entregáveis reais e limitações conhecidas. Materializa a verdade do disco: `CMksTradeManager`+sizers em `Core/Trade/`, `CMksRiskManager`+`CMksRiskGatedBroker` em `Core/Risk/`, `CMksAccountSnapshot` em `Core/Account/`, `CMksMt5PositionBook` em `Core/Position/`, log-diff via `tools/verify-parity.ps1`. Auditoria 2026-05-25 disparou a sincronização.
- `docs/ROADMAP.md` — Fase 7 (StressLab) reclassificada para **Concluída com limitações materiais documentadas**. As 3 limitações críticas (latência sorteada mas não aplicada ao fill, `spreadMultiplier` mal composto com `CMksCostModel`, SL/TP não disparados pelo `CMksSimulatedBroker`) são pré-requisito explícito da Fase 9 — recriações sutis do eixo 3 do V5-POSTMORTEM. O `CStressLabEngine.mqh` originalmente previsto não foi materializado — arquitetura final dispensou orquestrador em favor de composição direta (broker simulado → wrapper StressLab).
- `docs/ROADMAP.md` — Fase 5 ganhou nota de escopo reduzido: `ATR-adjusted` e `Kelly fracionado` sizers não foram implementados (eixo de tamanho dinâmico já coberto no `CMksAtrBrickSizer`; Kelly fica para quando houver histórico de retornos realizados).
- `docs/ARCHITECTURE.md` §2 — árvore de diretórios sincronizada com o disco. Adicionadas pastas reais: `Core/Output/`, `Core/Trade/`, `Core/Risk/`, `Core/Position/`, `Core/Account/CMksAccountSnapshot`, `Core/Testing/Mocks/`, `MQL5/Indicators/MKS-ULTIMATE/`, `tools/`, `reference/`. Removida referência a `tests/` na raiz — testes reais vivem em `MQL5/Scripts/MKS-ULTIMATE/Tests/`. Convenções de slice (5a/5b/6.1/6.2/6.3) referenciadas nos comentários da árvore.
- `docs/ARCHITECTURE.md` §3 — ADR-012 ganha nota de esclarecimento marcando como **quitada** a dívida "mecanismo de comparação de proveniência em runtime": `CMksFileTickSource.BrokerMismatch()`/`AccountMismatch()` (slice 24b); estendida pelo `CMksMultiFileTickSource` (slice 24d-parte-2) com erros 807, 810, 811.

### Fixed
- `tools/verify-parity.ps1` — corrigido falso negativo iminente identificado em auditoria pré-empírica. O `.mksbk` tem `createdAtMsc` (8 bytes wall-clock no offset 184-191 do header) que diverge inerentemente entre live (`Producer.OnDeinit`) e replay (`Replayer.OnDeinit`). A versão anterior (Slice 24f, commit 29a32e0) fazia `fc /b` direto, o que teria dado fail garantido na validação empírica de amanhã mesmo com builder 100% determinístico. Reescrita: lê ambos arquivos como `[byte[]]`, ignora explicitamente o range 184-191, compara byte-a-byte manualmente. Em divergência, reporta offset exato + identifica o campo (`header.broker`, `brick[42].close`, etc.) + bytes em hex ao redor do ponto de divergência. Smoke test em 3 cenários (idênticos, só wall-clock diferente, brick[0].direction diferente) passa.

### Added
- `tools/compile-all.ps1` — sanity check headless que compila todos os 35 `.mq5` do projeto (Experts, Services, Scripts, Indicators) via MetaEditor64 e reporta consolidado. Auto-detecta terminal data path via `%APPDATA%\MetaQuotes\Terminal\` procurando junctions `MQL5\Include\MKS-ULTIMATE`. Modos `-Quiet` (só summary) e `-Editor <path>` (override do MetaEditor). Exit codes: 0 limpo, 1 erros, 2 warnings, 3 setup. Útil pré-commit grande ou após refactor amplo (complementa o `watch-compile.ps1` incremental).
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-024 (timestamps wall-clock no header do `.mksbk`). Refina a §regra 7c — `fc /b` byte-idêntico fica restrito ao **range fora de 184-191** do header (8 bytes de `createdAtMsc`). Documenta que o `.mkstick` tem 2 timestamps wall-clock (184-191 e 192-199) que devem ser excluídos em pipelines futuros de comparação de `.mkstick`. ADR-024 não é alterada; nota registra o caveat técnico.
- `docs/CHEATSHEET.md` §9 — seção nova "Fluxos do framework MKS-ULTIMATE". Cobre: validação headless via `compile-all.ps1`, watcher incremental, **pipeline canônico de paridade da ADR-024 passo-a-passo** (Producer + TickRecorder em paralelo → Replayer → `verify-parity.ps1`), localização dos arquivos do framework e das junctions, comandos para recriar junctions em máquina nova.

### Changed
- `MQL5/Include/MKS-ULTIMATE/Core/Log/CMksLogger.mqh` — `#define MKS_MODULE_LOGGER "Logger"` para consistência com o padrão do projeto (em vez de string literal `"Logger"` hardcoded na linha que escreve o META header). P3-1 da auditoria de 2026-05-24 endereçado.

### Added
- `tools/verify-parity.ps1` — Slice 24f: script PowerShell que executa a validação canônica da paridade (ADR-024 §regra 7). Recebe paths de `live.mksbk` e `replay.mksbk` (e opcionalmente `live.log` + `replay.log`), faz comparação de tamanho, `fc /b` byte-a-byte, e diff de linhas de decisão dos logs (após normalização de `ts` e `sessionStartMsc`). Exit codes: 0 = paridade verificada, 1 = `.mksbk` divergem, 2 = logs divergem, 3 = arquivo não encontrado. Fecha a dívida da Fase 8 (log-diff tool).
- `docs/PROTOCOLOS.md` — Protocolo 1 ganha item: "se o módulo toca paridade (RenkoBuilder, ITickSource, IClock, IBroker, estratégia, sink que escreve .mksbk), executar tools/verify-parity.ps1 antes de declarar pronto". Materializa ADR-024 §Consequências.
- `docs/REGRAS.md` §1.9 — Estratégia é replay-safe. Tabela completa de APIs MQL5 proibidas em código de estratégia (não em borda) com substituto obrigatório via interface injetada. Materializa ADR-024 §regra 6.
- `docs/ROADMAP.md` — Fase 4.5 (Tick Recorder + Replayer) inserida entre Fase 4 e Fase 5, status "Concluída — validação empírica end-to-end pendente". Lista os 11 entregáveis materializados em 2026-05-24, critério de saída e 2 riscos abertos (R4.5.1 validação empírica, R4.5.2 fill histórico via CopyTicksRange).
- `MQL5/Experts/MKS-ULTIMATE/Replayer.mq5` — Slice 24e: EA que monta o composition root do framework **fora do Strategy Tester** e replaya `.mkstick` pelo mesmo `CMksRenkoBuilder` que rodaria em live. Saída canônica: `replay_<sourceStem>_<TS>.mksbk` + `Replayer_<symbol>_<TS>.log`. Compará vel byte-a-byte contra `live.mksbk` via `fc /b` (validação canônica da paridade — ADR-024 §regra 7). Versão mínima: source → builder → writer; broker simulado, risk e trade manager entram quando estratégia existir (Fase 9). Inputs: `InpTickFilePath` para single, `InpTickFolder`+`InpSymbolPrefix` para multi; `InpBrickSizePts`, `InpInvalidTickLimit` (L), `InpThresholdLimit` (K) replicam exatamente os defaults do Producer (ADR-026/ADR-006/ADR-011). Loop em `OnTimer` com `InpTicksPerCycle=10000` ticks por chamada, `InpTimerMs=1` (throughput máximo MQL5). `EmitForming(false)` no builder para evitar milhões de callbacks de bar parcial inúteis durante replay. `OnTick` deliberadamente vazio — Replayer não consome live, feed vem do `.mkstick`. Listagem multi-file via `FileFindFirst/Next` com pattern `<folder>\<prefix>_*.mkstick` + `ArraySort` lexicográfico (== cronológico por ADR-024 §3). EOF dispara `EventKillTimer` + `ExpertRemove` automático.
- `MQL5/Include/MKS-ULTIMATE/Core/Data/CMksMultiFileTickSource.mqh` — Slice 24d-parte-2: `ITickSource` sobre conjunto ordenado de `.mkstick` (multi-day replay). Materializa ADR-024 §regra 4 último parágrafo. Política cross-file (todas FATAIS): symbol divergente reusa erro 807 (single-file fatal), broker/account divergente vira erro novo 811 (`MKS_ERR_DATA_MULTI_PROVENANCE_MISMATCH`), seq descontínua entre arquivos consecutivos vira erro novo 810 (`MKS_ERR_DATA_SEQ_DISCONTINUITY`). Proveniência vs conta corrente em runtime (broker/account WARN) é herdada do primeiro arquivo aberto, mesma política do single-file. Falhas detectadas durante `Next()` (que não tem out-param na `ITickSource`) ficam expostas via `SeqDiscontinuityDetected()`/`AdvanceFailureDetected()` — composition root distingue EOF natural de falha real.
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksMultiFileTickSource.mq5` — 6 cenários: leitura cross-file contínua 3 arquivos (12 ticks), descontinuidade de seq fatal (810), broker divergente fatal (811), lista vazia, ReplayClock acompanhando feed cross-file, single-file via multi (degenera para comportamento single).
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/ITickSource.mqh` — método `LastTickTimeMsc()` (pure virtual) adicionado à interface. Permite `CMksReplayClock` aceitar qualquer `ITickSource*` (single-file ou multi-file ou futuras), removendo o acoplamento ao `CMksFileTickSource` concreto. `CMksFileTickSource.LastTickTimeMsc()` ganhou `override`. Conformidade com ADR-004 §4 (todo método é virtual puro; nenhum concreto vive na interface — apenas estendemos com mais virtual puro).
- `MQL5/Include/MKS-ULTIMATE/Core/Clock/CMksReplayClock.mqh` — aceita `ITickSource*` em vez de `CMksFileTickSource*`. `IsReady()` usa `LastTickTimeMsc() > 0` em vez de `TicksRead() > 0` para reduzir a área de acoplamento. Comportamento equivalente para todos os sources reais (primeiro tick válido tem `timeMsc > 0`).
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — códigos `MKS_ERR_DATA_SEQ_DISCONTINUITY = 810` e `MKS_ERR_DATA_MULTI_PROVENANCE_MISMATCH = 811` materializados (faixa Data, ADR-024 §4).
- `MQL5/Services/MKS-ULTIMATE/TickRecorder.mq5` — Slice 24d: Service de captura de ticks crus em arquivo `.mkstick`, materializa ADR-024 §regra 2 e §regra 3. Single-symbol (operador roda N Services em paralelo para N símbolos), roll-over diário UTC (`<symbol>_YYYYMMDD.mkstick`), checkpoint periódico configurável (default: a cada 100 ticks ou 2s, o que vier primeiro). Anchor inicial via `SymbolInfoTick` para evitar reprocessar histórico via `CopyTicks(fromMsc=0)`. Dedup por `timeMsc <= último` e filtro mínimo (descarte de ticks com `bid<=0 && ask<=0` na captura). Em colisão de nome (reopen no mesmo dia após crash do Service): sufixo numérico até `InpMaxFileAttempts=100`, mesmo padrão do Producer (ADR-014 §4); append-com-validação de proveniência fica para fase futura se virar problema. Pasta nova `MQL5/Services/MKS-ULTIMATE/` na árvore.
- `MQL5/Include/MKS-ULTIMATE/Core/Data/CMksTickFileWriter.mqh` — métodos `Checkpoint(MksError&)` e `Flush()`. Checkpoint patcha `tickCount`/`timeMscFirst`/`timeMscLast` no header sem fechar o handle, depois faz seek de volta para o final do arquivo e força `FileFlush` — permite que um crash do Service deixe o arquivo parcialmente válido (o leitor lê exatamente os ticks acumulados até o último Checkpoint). `closedAtMsc` continua sendo gravado só no Close. `Flush()` força I/O para disco sem mexer no header (cheaper, mas não atualiza tickCount).
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — código `MKS_ERR_DATA_RECORDER_INIT_FAILED = 809` (faixa Data), materializa o slot 809 antes reservado por comentário como `REOPEN_INCOMPATIBLE`. Semântica diferente: como o Slice 24d usa sufixo numérico em colisão de nome (ADR-014 §4) em vez de append-com-validação, o caso "REOPEN_INCOMPATIBLE" deixa de existir; 809 cobre falhas de init do Service (SymbolSelect, ficha técnica inválida, etc.).
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-007 (WARN/ERROR rate-limited em hot path). Refina a §3 "hot path mudo" para reconhecer a categoria de uso já implementada no `Producer.mq5:241-263`: `WARN`/`ERROR` são aceitos em hot path desde que rate-limited explicitamente (caller-side, padrão `InpInvalidLogEvery=100`); `TRACE`/`DEBUG`/`INFO` permanecem proibidos sem exceção. Identificado em auditoria forense de 2026-05-24.
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/IClock.mqh` — método `IsReady()` (pure virtual) na interface. Live (`CMksMt5Clock`) sempre `true`; replay (`CMksReplayClock`) `true` somente após o primeiro tick válido; mock (`CMksFakeClock`) sempre `true`. Fecha a janela em que `NowMsc()` retornaria 0 silenciosamente antes do primeiro tick em replay, permitindo ao composition root validar prontidão antes da primeira decisão temporal da estratégia.
- `MQL5/Experts/MKS-ULTIMATE/Test_MksMt5BrokerLive.mq5` — guard contra anexar em conta real (`AccountInfoInteger(ACCOUNT_TRADE_MODE) == ACCOUNT_TRADE_MODE_REAL`). EA executa Send/Close reais — guard fecha porta de acidente em produção. Magic 13371337 continua como defesa complementar.

### Changed
- `MQL5/Include/MKS-ULTIMATE/Core/RenkoBuilder/CMksRenkoBuilder.mqh` — doc-comment 1 linha acima da declaração da classe ("Coração do framework — transforma stream de ticks em sequência determinística de bricks Renko. Caminho único entre live e replay."), conformidade com Protocolo 1 item "Classe tem doc-comment na declaração". Doc-comments adicionais em `ContinuationThreshold` e `ReversalThreshold` explicando a fórmula de cada limiar.
- `MQL5/Include/MKS-ULTIMATE/Core/Trade/CMksTradeManager.mqh` — comentário de definição de "point" corrigido: referência a `SymbolInfoDouble(SYMBOL_POINT)` substituída por `ISymbol::Point()`, evitando sugerir violação do Protocolo 9 (o módulo já consome `ISymbol` corretamente; só o comentário enganava).

### Added
- `docs/ARCHITECTURE.md` §3 — ADR-026 aceita: Producer classic-only. Remove `ENUM_MKS_GEOMETRY_TYPE` e os inputs `InpGeometryType`/`InpPro`/`InpPo` do Producer; hardcoda `MksGeometryClassic()` no OnInit. Fecha a porta do espaço de preços fictício do median pelo caminho de produção, sem remover capability do core. Registra a equivalência matemática `median S=X ≡ classic S=(1−PO)·X`. Substitui parcialmente as regras 1/5/7 da ADR-022. Naming do CS simplifica para `<symbol>.MKS_<sizeStr>`.

### Changed
- `MQL5/Include/MKS-ULTIMATE/Core/Types/RenkoGeometry.mqh` — default do construtor `MksRenkoGeometry()` muda de median (`0.5/0.5/1.0`) para classic (`0/0/1.0`). Fábricas `MksGeometryMedian/Classic/Custom` continuam disponíveis no core. Consequência da ADR-026 cláusula 2.
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — ~90 linhas removidas. Saem: enum `ENUM_MKS_GEOMETRY_TYPE`, inputs `InpGeometryType`/`InpPro`/`InpPo`, funções `BuildGeometry()` e `GeometryTypeName()`, switch no `BuildCustomSymbolName`. Entra: `MksGeometryClassic()` hardcoded no OnInit, `"Classic"` literal no painel UX, `"preset":"classic"` no log de starting. Naming do CS reduz a `<symbol>.MKS_<sizeStr>`. Consequência da ADR-026 cláusulas 1, 3, 6.

### Added
- `docs/V5-POSTMORTEM.md` — análise de causa-raiz do colapso do V5, baseada na leitura direta do código-fonte. Identifica quatro eixos de falha estrutural.
- `docs/ARCHITECTURE.md` §3 — ADR-004 aceita: polimorfismo em MQL5 via classe abstrata com métodos virtuais puros (em vez da keyword `interface` nativa), com sete convenções operacionais para toda interface do core (`I*`).
- `MQL5/Include/MKS-ULTIMATE/Core/Types/` — quatro tipos primitivos do core, autocontidos: `MksTick` (com `seq` como fonte de verdade do determinismo), `MksBrick` (com `triggerPrice`, `triggerTickId` e `Overshoot()` — resposta direta ao eixo 1 do V5-POSTMORTEM), `MksOrderRequest` e `MksExecutionResult` (preço real de preenchimento, não fictício).
- `docs/ARCHITECTURE.md` §5 — Convenções de nomenclatura registradas: sistema de prefixos `I` (interfaces), `CMks` (classes com estado), `Mks` (structs/tipos primitivos POD) e `ENUM_MKS_` (enums internos).
- `MQL5/Include/MKS-ULTIMATE/Core/Version.mqh` — versão única do framework em defines SemVer (`MAJOR`/`MINOR`/`PATCH`/`STR`), conforme ADR-001.
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/` — cinco interfaces do core (`IBroker`, `ITickSource`, `IClock`, `ILogger`, `IRenkoSink`), classes abstratas com métodos virtuais puros conforme ADR-004.
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da convenção 7 da ADR-004: out-params por referência não-const são permitidos.
- `docs/ARCHITECTURE.md` §3 — ADR-009 aceita: modelo de erro — tipo estruturado retornável, separado do logging, com localização automática via macro e códigos de erro particionados por faixa de módulo.
- `docs/ARCHITECTURE.md` §3 — ADR-010 aceita: arquitetura de parametrização do RenkoBuilder (geometria como valor via `MksRenkoGeometry`, tamanho via interface `IBrickSizer`) e escolha do mid como preço-condutor do motor.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — tipo de erro `MksError` (estruturado, retornável, separado do logging), enum `ENUM_MKS_ERROR_CODE` particionado por faixa de módulo, e a macro `MKS_SET_ERROR` de captura automática de localização. Implementa a ADR-009.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/RenkoGeometry.mqh` — struct `MksRenkoGeometry`, o triplo `(PO, PRO, revSizeRatio)` da geometria do brick, com `Validate` via `MksError` e as fábricas de preset `MksGeometryMedian`, `MksGeometryClassic` e `MksGeometryCustom`. Implementa a ADR-010. O enum `ENUM_MKS_ERROR_CODE` (`Error.mqh`) ganha `MKS_ERR_RENKO_INVALID_GEOMETRY` (código 100), primeiro da faixa RenkoBuilder.
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/IBrickSizer.mqh` — interface `IBrickSizer` (6ª interface do core): contrato de fornecimento do tamanho-base do brick Renko em pontos, com `SizePoints`, `IsReady` (prontidão de sizers derivados de dados, ex. ATR) e `Validate` via `MksError`. Implementa o eixo de tamanho da ADR-010. O método de alimentação de dados foi deliberadamente omitido — sua forma depende da cadência de recálculo do ATR, adiada para o `CAtrBrickSizer`; o raciocínio está no doc-comment da interface.
- `MQL5/Include/MKS-ULTIMATE/Core/RenkoBuilder/CMksFixedBrickSizer.mqh` — primeira implementação concreta de `IBrickSizer`: tamanho de brick fixo em pontos, definido na construção e constante durante toda a sessão (`IsReady()` sempre verdadeiro, sem warm-up); `Validate()` rejeita tamanho menor ou igual a zero. Pasta `Core/RenkoBuilder/` criada. Acompanha o código de erro `MKS_ERR_RENKO_INVALID_BRICK_SIZE = 101` em `Error.mqh`, faixa RenkoBuilder.
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-010: as classes concretas de sizer seguem o prefixo `CMks` da §5 (`CMksFixedBrickSizer`, `CMksAtrBrickSizer`) e o campo de geometria implementado é `revSizeRatio` (razão), não `revSizePct`.
- `docs/ARCHITECTURE.md` §3 — ADR-011 aceita: tratamento de cruzamento multi-threshold no RenkoBuilder — um tick que cruza vários thresholds produz um único `MksBrick` honesto (sem phantom bricks), marcado pelo campo inteiro `thresholdsCrossed`; limiar configurável K acima do qual o builder devolve `MksError` em vez de emitir.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Brick.mqh` — campo `int thresholdsCrossed` no `MksBrick` (1 = brick normal, >1 = brick multi-threshold; default 1, pois um brick cruza no mínimo um threshold). Implementa o eixo de marcação da ADR-011.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — código de erro `MKS_ERR_RENKO_THRESHOLD_LIMIT_EXCEEDED = 102` (faixa RenkoBuilder), para o estouro do limiar K do RenkoBuilder. Implementa o eixo de guarda de corrupção da ADR-011.
- `docs/ARCHITECTURE.md` §3 — ADR-006 aceita: tratamento de tick inválido no RenkoBuilder. Volume zero não é anomalia; o critério é `MksTick::IsValid()` (preço malformado) como guarda de entrada do builder. Tick inválido é descartado e reportado; sequência de inválidos consecutivos interrompe o builder. Dois códigos novos na faixa RenkoBuilder de `Error.mqh`: `MKS_ERR_RENKO_INVALID_TICK` e `MKS_ERR_RENKO_TICK_STREAM_CORRUPT`.
- `docs/ARCHITECTURE.md` §3 — nota de esclarecimento da ADR-011: geometria do primeiro brick. Sem brick anterior não há "continuação" nem "reversão"; o primeiro brick tem direção fixada pelo sinal do movimento inicial e todos os seus degraus usam `(1-PO)*size` (continuação). Para median a regra não altera valores; importa para presets assimétricos futuros. Comentário-âncora correspondente em `CMksRenkoBuilder.mqh` no caminho `!m_hasFirstBrick`.
- `docs/ARCHITECTURE.md` §3 — ADR-013 aceita: independência de broker e proveniência no rastro de auditoria. Framework é broker-agnóstico por construção — sem hardcode de identificador de broker em código de lógica, sem cláusula condicional ramificando por nome de broker. Símbolo é parâmetro (não constante literal). Todo artefato persistido ou comunicado carrega proveniência `(broker, account, symbol)` capturada em runtime via `AccountInfoString(ACCOUNT_COMPANY)` e `AccountInfoInteger(ACCOUNT_LOGIN)`. Detecção e perfil estruturado de broker ficam como dívida explícita pós-Slice 3, quando houver evidência empírica do que de fato varia entre brokers.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Tick.mqh` — campo `uint flags` no `MksTick`, bitmask dos `TICK_FLAG_*` do MT5. Implementa a consequência de tipo da ADR-012 §4: os flags são informação de auditoria irreversível — o builder atual (mid-driven, ADR-010) não consome, mas estratégias e camada de execução futuras podem precisar, e a captura é one-shot. `ValidateBuilderOnRealTicks.mq5` atualizado para propagar `mt.flags` em `ToMksTick`.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — faixa `800–899` reservada para `Data` (TickSource, serializadores, integridade de header) per ADR-012 §Consequências. Comentário-âncora apenas — os códigos concretos serão definidos quando o serializador / `ITickSource` de backtest forem implementados (ADR-012 explicitamente proíbe fixar números no vazio).
- `MQL5/Include/MKS-ULTIMATE/Core/Data/` — primeira fatia do produtor (Slice 3a): formato binário `.mksbk` para persistência de bricks com header de proveniência. Layout fixo de 256 bytes (200 usados, 56 reservados zero-fill para crescer sem bumping de version), brick record de 72 bytes, little-endian. Magic `MKSBRK01` ASCII embute version major.minor.
  - `BrickFileFormat.mqh` — constantes (magic, sizes, version, offsets) e doc-comment com layout campo-a-campo.
  - `CMksBrickFileWriter.mqh` — Open / WriteHeader / WriteBrick / Close. Header é gravado com `brickCount=0` e tempos zerados; Close faz seek e patcheia com valores reais. `createdAtMsc` aceita override para golden file test reproduzível.
  - `CMksBrickFileReader.mqh` — Open valida magic, formatVersion, recordSize, headerSize e tamanho total do arquivo == header + N*record (rejeita truncamento). Expõe proveniência via getters (broker, account, symbol, digits, geometry, brickSize, brickCount, times).
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — códigos concretos da faixa Data: `MKS_ERR_DATA_FILE_IO=800`, `MKS_ERR_DATA_INVALID_MAGIC=801`, `MKS_ERR_DATA_UNSUPPORTED_VERSION=802`, `MKS_ERR_DATA_HEADER_INVALID=803`, `MKS_ERR_DATA_TRUNCATED=804`, `MKS_ERR_DATA_STATE_INVALID=805`. Substitui o comentário-âncora da faixa, agora que o serializador existe (ADR-012 §Consequências: códigos exatos quando o serializador for implementado).
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksBrickFile.mq5` — golden file test com 4 cenários: roundtrip campo-a-campo (proveniência + bricks); golden file rewrite (write → read → rewrite produz bytes idênticos); rejeição de magic inválido (corrompe primeiro byte e verifica `MKS_ERR_DATA_INVALID_MAGIC`); leitura além de `brickCount` falha com `MKS_ERR_DATA_TRUNCATED`.
- `docs/ARCHITECTURE.md` §3 — ADR-014 aceita: política de rotação e nomenclatura do `.mksbk`. Cinco cláusulas — arquivo novo a cada `OnInit` (anti-`SyncWithExisting` do V5), naming mínimo `<symbol>_<YYYYMMDDTHHMMSS>.mksbk` (proveniência no header, não no nome), sub-pasta única `MKS-ULTIMATE\Bricks\`, guarda contra colisão com retry de sufixo, sem limpeza automática (retenção é operacional).
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — código `MKS_ERR_DATA_FILE_EXISTS = 806` (faixa Data) consumido por `CMksBrickFileWriter::Open` via `FileIsExist` (ADR-014 §4 cláusula 4).
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — Slice 3b: EA produtor que funde `CMksRenkoBuilder` + `CMksFixedBrickSizer` + `CMksBrickFileWriter` num único programa. Combate ao eixo 2 do V5 — histórico (`CopyTicksRange` em OnInit) e live (`OnTick`) atravessam o mesmo motor e gravam no mesmo `.mksbk`. Símbolo chart-bound, proveniência via `AccountInfoString(ACCOUNT_COMPANY)`/`AccountInfoInteger(ACCOUNT_LOGIN)` gravada no header. `OnDeinit` patcheia header com `brickCount`/`timeFirst`/`timeLast`/`createdAt`. Tratamento de erro: 102 sempre logado, 103 rate-limited, 104 para o stream. Retry de sufixo no naming (`kMaxAttempts=100`, ADR-014 §4).
- `MQL5/Scripts/MKS-ULTIMATE/ValidateProducerOutput.mq5` — script de validação: lê o `.mksbk` mais recente via `CMksBrickFileReader`, imprime proveniência, geometria, distribuição de `thresholdsCrossed` (M=1, M=2..5, M=6..10, M>10), primeiros/últimos bricks. Fecha o round-trip Producer → arquivo → reader.
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — segundo sink: `CCustomSymbolSink` (envelope de `IRenkoSink`) replica cada brick em um Custom Symbol via `CustomRatesUpdate`. Composite `CMultiSink` agrega `writerSink` + `csSink` e despacha para ambos. `EnsureCustomSymbolReady` cria/recupera o CS via `CustomSymbolCreate` e replica propriedades imutáveis (`SYMBOL_DIGITS`, `SYMBOL_CHART_MODE=BID`, `SYMBOL_POINT`, `TICK_SIZE`, `TICK_VALUE`, `CONTRACT_SIZE`, `CURRENCY_*`). Slot M1 monotônico (`nextBarTime += 60` por brick) evita colisão de timestamp — `CustomRatesUpdate` é upsert por `time`.
- `docs/ARCHITECTURE.md` §2 — pasta `MQL5/Services/` adicionada à árvore-alvo. Coletores de tick em background e workers independentes de gráfico (sinal de auditoria de alinhamento MQL5).
- `docs/ARCHITECTURE.md` §4 — entradas para ADRs 015, 016, 017 e 018 adicionadas à fila de decisões pendentes (sinal da auditoria MQL5), com descrição e ordem de bloqueio.
- `docs/PROTOCOLOS.md` — **Protocolo 9** adicionado: tabela completa de chamadas API globais proibidas em código de lógica (tempo, símbolo, conta, identidade, séries, execução, Custom Symbol) com substituto canônico para cada função. Protocolo 1 ganha referência ao Protocolo 9. Sinal-novo da auditoria de alinhamento MQL5.
- `docs/ARCHITECTURE.md` §3 — ADR-015 aceita: Strategy Tester nativo do MQL5 é ferramenta de instrumentação, não fonte de verdade. Decisão filosófica pura — sem código consequência. Bloqueava a ADR-018 (vetou alternativa `iATR` nativo dentro do StrategyTester).
- `docs/ARCHITECTURE.md` §3 — ADR-007 aceita: formato e destino do log estruturado. JSON-line com schema fixo, destino dual (`Print` ao journal + `FileWrite` a arquivo por sessão), hot path mudo (não logar dentro de `OnTick`/`IngestTick`), política de níveis na borda. Header META com proveniência. Limitação registrada: `.000Z` fixo em `FormatTimestamp` (MQL5 não expõe API para "TimeCurrent com millis" fora de `MqlTick.time_msc`).
- `docs/ARCHITECTURE.md` §3 — ADR-018 aceita: cálculo do ATR no `CMksAtrBrickSizer`. ATR sobre **bricks fechados** (não barras de tempo), Wilder smoothing, warm-up via `defaultSizePoints` para evitar deadlock com o builder. Vetou alternativa `iATR` nativo (mata paridade backtest/live por causa do ADR-015).
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/IBrickSizer.mqh` — interface ganha método `OnBrick(MksBrick&)` pure virtual (ADR-018 §2). `CMksRenkoBuilder.EmitBrick` chama `m_sizer.OnBrick(brick)` após notificar o sink. `CMksFixedBrickSizer` ganha implementação no-op (semanticamente neutro para sizer constante).
- `MQL5/Include/MKS-ULTIMATE/Core/Log/CMksLogger.mqh` — primeira implementação concreta de `ILogger` (ADR-007). JSON-line com schema fixo, escape de paths via `MksJsonEscape`, header META com proveniência (`broker`, `account`, `symbol`, `digits`, `geometry`), helpers `Trace/Debug/Info/Warn/Error`. `ILogger` da Fase 1 ganhou nova assinatura `Log(level, module, msg, ctxJson)` — sem consumidor real prévio, sem breakage. `Producer.mq5` refatorado: todos `Print`/`PrintFormat` substituídos por chamadas do logger; input `InpLogToFile`. Códigos `MKS_ERR_LOG_FILE_IO=600` e `MKS_ERR_LOG_STATE_INVALID=601` em `Error.mqh` (faixa Log).
- `MQL5/Include/MKS-ULTIMATE/Core/RenkoBuilder/CMksAtrBrickSizer.mqh` — segunda implementação concreta de `IBrickSizer` (ADR-018). ATR Wilder sobre bricks fechados, warm-up via `defaultSizePoints`, `IsReady=true` sempre (anti-deadlock), parâmetros: `atrPeriod=14`, `multiplier=0.5`, `defaultSize`, clamp `min/max`. Cadência de recálculo a cada brick fechado (consequência do `IBrickSizer.OnBrick`).
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksAtrBrickSizer.mq5` — 11 cenários, 72 assertions (Validate, warm-up, transição no N-ésimo brick, fórmula TR primeiro/com-gap, Wilder smoothing pós-warm-up, clamp min/max, determinismo entre 2 sizers).
- `docs/ARCHITECTURE.md` §3 — ADR-016 aceita: interfaces `ISymbol` (16 métodos de ficha técnica do instrumento) e `IAccount` (10 métodos de estado da conta), get-on-demand, sem snapshot. Define `Core/Symbol/` e `Core/Account/` como pastas novas. Fecha uma das duas ADRs estruturais que sobravam pendentes pós-auditoria.
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/ISymbol.mqh` — interface `ISymbol` (Name, Digits, Point, TickSize, TickValue, ContractSize, VolumeMin/Max/Step, StopsLevel, FreezeLevel, FillingMode, BaseCurrency, ProfitCurrency, MarginCurrency).
- `MQL5/Include/MKS-ULTIMATE/Core/Interfaces/IAccount.mqh` — interface `IAccount` (Login, Company, Currency, Balance, Equity, Margin, FreeMargin, Leverage, MarginMode, TradeMode).
- `MQL5/Include/MKS-ULTIMATE/Core/Symbol/CMksMt5Symbol.mqh` — implementação real de `ISymbol` via `SymbolInfo*`. Construtor `(string)`. Pasta `Core/Symbol/` criada.
- `MQL5/Include/MKS-ULTIMATE/Core/Account/CMksMt5Account.mqh` — implementação real de `IAccount` via `AccountInfo*`. Usa `ACCOUNT_MARGIN_FREE` (não o deprecated `ACCOUNT_FREEMARGIN`). Pasta `Core/Account/` criada.
- `MQL5/Experts/MKS-ULTIMATE/Producer.mq5` — refactor: composition root instancia `g_iSymbol`/`g_iAccount`; ~8 chamadas a `SymbolInfo*`/`AccountInfo*` migram para as interfaces. Header META do `.log` passa a usar `g_iAccount.Company()`, `g_iAccount.Login()`, `g_iSymbol.Digits()`.
- `docs/ARCHITECTURE.md` §3 — ADR-017 aceita: modelo de confirmação de execução do `CMksMt5Broker`. 8 decisões — `Send`/`Close` síncronos lógicos (bloqueia até `OnTradeTransaction` reportar `DEAL_ADD`, `fillPrice` via `HistoryDealGetDouble`), broker per-símbolo, timeout 5s configurável sem retry automático em timeout, filling pré-detectado com fallback, margin mode dual (netting/hedging), retry interno (REQUOTE/PRICE_CHANGED/PRICE_OFF, 3 tentativas, backoff 100ms), deviation default 10 points, `CMksCostModel` em classe separada. Última ADR estrutural pendente do MVP.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/ExecutionResult.mqh` — `MksExecutionResult` ganha `swap`, `dealId`, `attempts` (ADR-017 §Consequências). Construtor zera; consumidores existentes não quebram.
- `MQL5/Include/MKS-ULTIMATE/Core/Types/Error.mqh` — faixa Broker 200–299 estreada com 4 códigos: `MKS_ERR_BROKER_TIMEOUT=200`, `MKS_ERR_BROKER_INVALID_FILL=201`, `MKS_ERR_BROKER_RETRY_EXHAUSTED=202`, `MKS_ERR_BROKER_NOT_INITIALIZED=203`.
- `MQL5/Include/MKS-ULTIMATE/Core/Broker/CMksCostModel.mqh` — modelo de custos plugável (ADR-017 §8). Determinístico, sem RNG. Parâmetros: `spreadPoints`, `slippagePoints`, `commissionPerLot`, `swapLongPerDay`, `swapShortPerDay` (todos default 0 = sem custos). API: `Validate(err)`, `FillPriceFor(side, mid, pointValue)`, `Commission(lots)`, `SwapForDays(side, lots, days)`. BUY paga `mid + halfSpread + slip`; SELL recebe `mid - halfSpread - slip`. Pasta `Core/Broker/` criada.
- `MQL5/Include/MKS-ULTIMATE/Core/Broker/CMksSimulatedBroker.mqh` — broker simulado em memória (ADR-017). Implementa `IBroker`. Construtor recebe `ISymbol*` + `CMksCostModel*`. EA chama `OnTick(tick)` antes de `Send/Close`. Simplificações v1: assume HEDGING (cada Send abre position nova); SL/TP armazenados sem auto-close (Trade Manager futuro monitora); sem cálculo automático de swap acumulado.
- `MQL5/Scripts/MKS-ULTIMATE/Tests/Test_CMksSimulatedBroker.mq5` — 12 cenários, 51 assertions (not_initialized, invalid_request, send sem custos/com spread/com slippage/com commission, SL/TP BUY+SELL, close full+partial, close inexistente, modify, hedging com 3 sends, determinismo entre 2 brokers). `CFakeSymbol` inline no teste (mock formal vem com a ADR-005).
- `MQL5/Include/MKS-ULTIMATE/Core/Broker/CMksMt5Broker.mqh` — broker real contra API MT5 (ADR-017). Implementa `IBroker`. Construtor recebe `ISymbol*`, `IAccount*`, `magic`, `deviation`, `timeoutMs`, `maxRetries`, `retryDelayMs`. `Init()` detecta filling preferido (FOK→IOC→RETURN) e margin mode. `Send`/`Close`: `OrderSend` síncrono + retry interno em REQUOTE/PRICE_CHANGED/PRICE_OFF + fallback automático de filling em INVALID_FILL. Caminho síncrono direto via `HistoryDealSelect(res.deal)` imediatamente após `OrderSend` retornar DONE (lição empírica: Sleep loop esperando `OnTradeTransaction` deu TIMEOUT em 5s mesmo com ordem executada em 129ms, por causa do single-threaded do MQL5). `OnTradeTransactionEvent` permanece como fallback. Validação empírica: Send+Close em XAUUSDm/Exness demo, 524ms total.
- `MQL5/Experts/MKS-ULTIMATE/Test_MksMt5BrokerLive.mq5` — EA de validação empírica do `CMksMt5Broker` em conta demo. Send + Close, sem timeout.
- `docs/ARCHITECTURE.md` §3 — ADR-005 aceita: framework próprio mínimo para testes unitários do core, em `Core/Testing/`. Três artefatos — `Asserts.mqh` (vocabulário uniforme com tolerância default explícita), `TestRunner.mqh` (singleton + macro `MKS_RUN(funcName)` ligando nome do teste ao nome da função), `Core/Testing/Mocks/` (mocks reutilizáveis). 5 alternativas rejeitadas (MQL5-Unit, só extrair helpers, auto-registro via function pointer, contexto por teste, mocks em `Scripts/Tests/`). Redefine o critério de saída efetivo da Fase 3: framework + 4 testes existentes migrados sem perda de cobertura.
- `docs/ARCHITECTURE.md` §3 — ADR-024 aceita: captura e replay de ticks crus via formato binário `.mkstick` (header 256B + record 64B, espelhando `.mksbk`), Service `TickRecorder` (`MQL5/Services/MKS-ULTIMATE/`) e EA `Replayer` (`MQL5/Experts/MKS-ULTIMATE/`) que monta o composition root completo fora do Strategy Tester. Materializa o trabalho pendente de ADR-012 (§5 layout binário; §Consequências captura/consumo) e ADR-015 (§Consequências engine de backtest fora do tester). Fixa paridade bit-a-bit do feed Renko e da decisão da estratégia entre live e backtest; paridade de execução continua modelada pelo `CMksSimulatedBroker`/StressLab. Cláusula §1.9 futura na REGRAS.md vinculando estratégia replay-safe (proibições de runtime: `TimeCurrent`, `MathRand`, `_Symbol`, `SymbolInfo*`, `AccountInfo*`, leitura de bars), validação canônica via `tools/verify-parity.ps1` (fc/b dos `.mksbk` + diff das linhas `decision=*` dos logs) fechando a dívida da Fase 8 (log-diff tool). Implementação em 6 commits subsequentes (Slices 24a–24f), com reconciliação dos códigos de erro `MKS_ERR_TICKFILE_*` dentro da faixa 800–899 já parcialmente ocupada pela ADR-012.
- `docs/ARCHITECTURE.md` §3 — ADR-008 aceita: tratamento de reabertura de mercado (gap de fim-de-semana) no `CMksRenkoBuilder`. Decisão de fixar como deliberado o status quo: primeiro tick pós-gap é tratado como qualquer outro tick; mecanismo da ADR-011 (multi-threshold) absorve o salto; `K` é a guarda contra gap patológico. Sem detecção temporal, sem brick parcial pré-gap, sem flag em `MksBrick`. Quatro alternativas rejeitadas (detecção temporal, brick com flag, brick sintético de fechamento, status quo informal). Apoiada em evidência empírica do `CHECKPOINT-2026-05-20-slice2.md §6` — gap de 49h em XAU/Exness absorvido como brick M=2, zero erros. Quita a única dívida formal pendente na §4. Sem código alterado.

### Changed
- `docs/Projeto.md` §2 — causa-raiz do V5 reescrita: de "divergência silenciosa entre backtest e live" (sintoma) para a causa estrutural de quatro eixos.
- `docs/Projeto.md` §6 — referência ao V5 corrigida: V5 tinha engine Renko própria e não dependia de indicador de caixa-preta (essa é característica do AzInvest).
- `docs/ROADMAP.md` — removida afirmação incorreta de que o V5 dependia de indicador de caixa-preta; seção "Lições aprendidas do V5" reescrita com base nos quatro eixos do post-mortem.
- `docs/REGRAS.md` §1.7 — regra de paridade backtest/live ampliada: de proibição de `if(MQL5_TESTING)` para proibição de qualquer condicional de ambiente na lógica de trading.
- `docs/ARCHITECTURE.md` §4 — entrada "ADR-004 (pendente)" removida da fila de decisões pendentes (decisão registrada na seção 3).
- Documentação sincronizada com o estado real do projeto: status das fases no `ROADMAP.md` (Fases 0 e 1 concluídas, Fase 2 em andamento), seção de estrutura do `CLAUDE.md` apontando para `ARCHITECTURE.md` §2, lista de documentos do `README.md` completada, campo `@depends_on` adicionado ao formato de header em `CLAUDE.md` e `PROTOCOLOS.md`, e comentários da árvore de diretórios em `ARCHITECTURE.md` §2.
- `docs/V5-POSTMORTEM.md` §3.2 — tabela do Eixo 2 corrigida: o Generator do V5 tem dois caminhos produtores (`ProcessTicks` e `ProcessM1`, com fallback silencioso para M1 OHLC), totalizando cinco produtores, não quatro. Revisão registrada na §7.
- `docs/ROADMAP.md` — Fases 2, 3 e 4 realinhadas com a realidade do código. **Fase 2** (RenkoBuilder) passa de "Em andamento" para **Concluída** — `CMksRenkoBuilder` + `CMksFixedBrickSizer` + `CMksAtrBrickSizer` implementados, 4 casos resolvidos via ADRs 010/011/006, determinismo validado em 428 assertions. **Fase 3** (Testes do core) passa de "Não iniciada" para **Parcialmente concluída** — 648 assertions distribuídas em 4 suítes, mas inline; framework formal aberto até a ADR-005 ser materializada. **Fase 4** (Broker abstractions) passa de "Não iniciada" para **Concluída** — ADR-017 inteira materializada em código testado (`CMksMt5Broker` validado em XAUUSDm/Exness demo em 524ms, `CMksSimulatedBroker` em 51 assertions, `CMksCostModel` plugável).
- `docs/PROTOCOLOS.md` — Protocolo 2 ganha consequência da ADR-015: backtest para release não usa o Strategy Tester como gold standard; números do release vêm do framework próprio (arquivo de captura + script de replay). Protocolo 9 atualizado: linhas de Mercado/Conta apontam para `ISymbol.*`/`IAccount.*` como disponíveis (não mais "pendentes"), com referência a `Core/Symbol/CMksMt5Symbol` e `Core/Account/CMksMt5Account`.
- `docs/ARCHITECTURE.md` §2 — pastas `Core/Symbol/`, `Core/Account/`, `Core/Broker/`, `Core/Log/`, `Core/Data/` adicionadas à árvore conforme materialização das ADRs 016, 017, 007 e 012.
- `docs/ARCHITECTURE.md` §4 — entradas "ADR-005, 007, 015, 016, 017, 018, 014, 012, 011, 006 (pendentes)" removidas da fila de decisões pendentes conforme cada ADR foi aceita. Restante na fila: ADR-008 apenas.

## [6.0.0-alpha] - 2026-04-24

### Added
- Estrutura inicial do projeto MKS-ULTIMATE
- README.md com visão geral e princípio norteador
- .gitignore específico para projetos MQL5
- CLAUDE.md com contexto persistente para Claude Code
- Pasta docs/ para documentação viva
- CHANGELOG.md seguindo Keep a Changelog
